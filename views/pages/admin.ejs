<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
<title>Admin — <%= settings.siteName %></title>
<script src="https://cdn.tailwindcss.com"></script>
<script src="https://code.iconify.design/iconify-icon/1.0.7/iconify-icon.min.js" defer></script>
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=Orbitron:wght@700;900&display=swap" rel="stylesheet">
<script>tailwind.config={theme:{extend:{fontFamily:{inter:['Inter','sans-serif'],orbitron:['Orbitron','sans-serif']}}}}</script>
<style>
:root{--bg:#0b0b1a;--card:#0f0f25;--card2:#12122a;--border:rgba(255,255,255,.07);--accent:#6366f1;}
*{margin:0;padding:0;box-sizing:border-box;}
html,body{font-family:'Inter',sans-serif;background:var(--bg);color:#e0e0e0;overflow-x:hidden;}
body{padding-bottom:80px;}
::-webkit-scrollbar{width:4px;height:4px;}
::-webkit-scrollbar-track{background:var(--bg);}
::-webkit-scrollbar-thumb{background:#2a2a4a;border-radius:3px;}

body::before{content:'';position:fixed;inset:0;background-image:linear-gradient(rgba(124,47,214,.02) 1px,transparent 1px),linear-gradient(90deg,rgba(124,47,214,.02) 1px,transparent 1px);background-size:40px 40px;pointer-events:none;z-index:0;}
body>*{position:relative;z-index:1;}

@keyframes fadeInUp{from{opacity:0;transform:translateY(12px);}to{opacity:1;transform:translateY(0);}}
@keyframes slideUp{from{transform:translateY(100%);opacity:0;}to{transform:translateY(0);opacity:1;}}
@keyframes spin{to{transform:rotate(360deg);}}
@keyframes blink{0%,100%{opacity:1;}50%{opacity:.4;}}

.fade-in{animation:fadeInUp .3s ease-out both;}
.animate-spin{animation:spin 1s linear infinite;}
.shimmer-text{background:linear-gradient(135deg,#38bdf8,#d8b4fe);-webkit-background-clip:text;-webkit-text-fill-color:transparent;}

.neon-card{background:var(--card);border:1px solid var(--border);border-radius:14px;transition:border-color .2s;}
.neon-card:hover{border-color:rgba(124,47,214,.25);}

.stat-card{background:linear-gradient(135deg,rgba(124,47,214,.1),rgba(124,47,214,.05));border:1px solid rgba(124,47,214,.2);border-radius:14px;padding:16px;transition:all .2s;}
.stat-card:hover{border-color:rgba(124,47,214,.4);transform:translateY(-2px);}

.hm-input,.form-input{width:100%;padding:10px 14px;border-radius:10px;background:rgba(255,255,255,.05);border:1.5px solid rgba(255,255,255,.1);color:#fff;font-size:13px;transition:all .2s;font-family:'Inter',sans-serif;}
.hm-input:focus,.form-input:focus{outline:none;border-color:rgba(124,47,214,.6);background:rgba(124,47,214,.06);box-shadow:0 0 0 3px rgba(124,47,214,.1);}
.hm-input::placeholder,.form-input::placeholder{color:#52525b;}
select.hm-input{appearance:none;cursor:pointer;}
textarea.hm-input{resize:vertical;min-height:80px;}

.btn-primary{background:linear-gradient(135deg,#6366f1,#6366f1);color:#fff;padding:10px 18px;border-radius:10px;font-weight:700;font-size:12px;border:none;cursor:pointer;transition:all .2s;display:inline-flex;align-items:center;gap:6px;}
.btn-primary:hover{filter:brightness(1.15);transform:translateY(-1px);}
.btn-danger{background:linear-gradient(135deg,#ef4444,#dc2626);color:#fff;padding:7px 14px;border-radius:8px;font-weight:600;font-size:11px;border:none;cursor:pointer;transition:filter .2s;display:inline-flex;align-items:center;gap:4px;}
.btn-danger:hover{filter:brightness(1.15);}
.btn-success{background:linear-gradient(135deg,#10b981,#059669);color:#fff;padding:7px 14px;border-radius:8px;font-weight:600;font-size:11px;border:none;cursor:pointer;transition:filter .2s;display:inline-flex;align-items:center;gap:4px;}
.btn-success:hover{filter:brightness(1.15);}
.btn-warning{background:linear-gradient(135deg,#f59e0b,#d97706);color:#fff;padding:7px 14px;border-radius:8px;font-weight:600;font-size:11px;border:none;cursor:pointer;transition:filter .2s;display:inline-flex;align-items:center;gap:4px;}

.data-table{width:100%;border-collapse:collapse;}
.data-table th{background:rgba(124,47,214,.1);padding:10px 12px;text-align:left;font-size:10px;font-weight:800;color:#38bdf8;text-transform:uppercase;letter-spacing:.6px;white-space:nowrap;}
.data-table td{padding:10px 12px;border-bottom:1px solid rgba(255,255,255,.05);font-size:12px;vertical-align:middle;}
.data-table tr:hover td{background:rgba(124,47,214,.04);}

.badge{display:inline-flex;align-items:center;gap:3px;padding:3px 8px;border-radius:6px;font-size:10px;font-weight:700;text-transform:uppercase;white-space:nowrap;}
.badge-done{background:rgba(16,185,129,.15);color:#34d399;border:1px solid rgba(16,185,129,.25);}
.badge-pending{background:rgba(234,179,8,.15);color:#facc15;border:1px solid rgba(234,179,8,.25);}
.badge-inactive{background:rgba(239,68,68,.15);color:#f87171;border:1px solid rgba(239,68,68,.25);}
.badge-failed{background:rgba(239,68,68,.15);color:#f87171;border:1px solid rgba(239,68,68,.25);}
.badge-featured{background:rgba(124,47,214,.15);color:#d8b4fe;border:1px solid rgba(124,47,214,.25);}

.tab-pane{display:none;}
.tab-pane.active{display:block;animation:fadeInUp .3s ease-out both;}

.modal-wrap{display:none;position:fixed;inset:0;z-index:80;}
.modal-wrap.open{display:flex;align-items:flex-end;justify-content:center;}
@media(min-width:640px){.modal-wrap.open{align-items:center;}}
.modal-bg{position:absolute;inset:0;background:rgba(0,0,0,.75);}
.modal-box{position:relative;background:var(--card);border:1px solid rgba(124,47,214,.2);width:100%;max-height:90vh;overflow-y:auto;border-radius:20px 20px 0 0;animation:slideUp .25s ease-out both;}
@media(min-width:640px){.modal-box{border-radius:16px;max-width:580px;}}
.modal-box::-webkit-scrollbar{width:3px;}
.modal-box::-webkit-scrollbar-thumb{background:#2a2a4a;border-radius:2px;}
.modal-handle{width:40px;height:4px;background:rgba(255,255,255,.12);border-radius:2px;margin:12px auto 0;}

#toast-container{position:fixed;top:12px;right:12px;z-index:9999;display:flex;flex-direction:column;gap:8px;}
.toast{display:flex;align-items:center;gap:8px;padding:10px 14px;border-radius:12px;background:var(--card);box-shadow:0 8px 32px rgba(0,0,0,.5);font-size:12px;max-width:280px;border:1px solid;animation:fadeInUp .3s ease-out both;}

#admin-nav{position:fixed;bottom:0;left:0;right:0;z-index:70;background:rgba(8,8,22,.98);border-top:1px solid rgba(124,47,214,.15);height:68px;display:flex;align-items:center;justify-content:space-around;padding:0 4px 6px;box-shadow:0 -4px 20px rgba(0,0,0,.5);}
.nav-btn{display:flex;flex-direction:column;align-items:center;gap:3px;padding:6px 8px;border-radius:12px;cursor:pointer;transition:all .2s;flex:1;max-width:64px;border:1.5px solid transparent;-webkit-tap-highlight-color:transparent;position:relative;}
.nav-btn .n-icon{font-size:20px;color:#52525b;transition:color .2s;line-height:1;}
.nav-btn .n-label{font-size:9px;font-weight:700;color:#52525b;transition:color .2s;white-space:nowrap;}
.nav-btn.active .n-icon{color:#38bdf8;}
.nav-btn.active .n-label{color:#38bdf8;}
.nav-btn.active{background:rgba(124,47,214,.12);border-color:rgba(124,47,214,.25);}
.nav-btn:active{transform:scale(.88);}
.nav-dot{position:absolute;top:4px;right:6px;width:7px;height:7px;background:#ef4444;border-radius:50%;border:1.5px solid var(--bg);animation:blink 2s infinite;}

.page-top{padding:12px 16px;background:rgba(8,8,22,.95);border-bottom:1px solid var(--border);position:sticky;top:0;z-index:30;}
.prod-card{background:var(--card2);border:1px solid var(--border);border-radius:12px;padding:14px;transition:border-color .2s;display:flex;align-items:center;gap:12px;}
.prod-card:hover{border-color:rgba(124,47,214,.3);}
.section-label{font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.8px;color:#52525b;margin-bottom:10px;display:flex;align-items:center;gap:6px;}
.section-label::before{content:'';width:16px;height:2px;background:var(--accent);border-radius:2px;display:block;}
.setting-row-label{font-size:11px;font-weight:700;color:#a1a1aa;margin-bottom:8px;}
</style>
</head>
<body class="font-inter">

<div id="toast-container"></div>

<!-- HEADER -->
<div id="binid-alert" class="hidden px-4 py-2 text-[11px] font-bold text-center" style="background:rgba(234,179,8,.15);border-bottom:1px solid rgba(234,179,8,.3);color:#fbbf24;">
    <iconify-icon icon="mdi:alert" class="align-middle mr-1"></iconify-icon>
    <span id="binid-msg"></span>
</div>

<div class="page-top flex items-center justify-between">
    <div class="flex items-center gap-2.5">
        <div class="w-8 h-8 rounded-lg bg-gradient-to-br from-purple-600 to-indigo-700 flex items-center justify-center flex-shrink-0">
            <iconify-icon icon="mdi:shield-crown" class="text-white text-base"></iconify-icon>
        </div>
        <div>
            <div class="font-bold text-sm font-black shimmer-text">ADMIN PANEL</div>
            <div class="text-[9px] text-zinc-600"><%= settings.siteName %></div>
        </div>
    </div>
    <div class="flex items-center gap-2">
        <span class="text-[10px] text-zinc-500 hidden sm:block">
            <span class="text-emerald-400 font-bold"><%= stats.doneTransactions %></span> done ·
            <span class="text-yellow-400 font-bold"><%= stats.pendingTransactions %></span> pending
        </span>
        <a href="/" class="flex items-center gap-1.5 px-3 py-1.5 rounded-lg border border-white/10 text-zinc-400 hover:text-white hover:bg-white/5 transition-all text-xs">
            <iconify-icon icon="mdi:home" class="text-sm"></iconify-icon>
            <span class="hidden sm:inline">Beranda</span>
        </a>
        <a href="/logout" onclick="clearAndLogout(event)" class="flex items-center gap-1.5 px-3 py-1.5 rounded-lg border border-red-500/20 text-red-400 hover:bg-red-500/10 transition-all text-xs">
            <iconify-icon icon="mdi:logout" class="text-sm"></iconify-icon>
        </a>
    </div>
</div>

<!-- MAIN -->
<div class="max-w-4xl mx-auto px-4 py-5">

<!-- ── DASHBOARD ── -->
<div id="pane-dashboard" class="tab-pane active">
    <div class="grid grid-cols-2 sm:grid-cols-3 gap-3 mb-3">
        <div class="stat-card fade-in" style="background:linear-gradient(135deg,rgba(124,47,214,.12),rgba(124,47,214,.06));border-color:rgba(124,47,214,.25);">
            <div class="flex items-center justify-between mb-2">
                <span class="text-[10px] text-zinc-500 font-semibold uppercase tracking-wide">Produk</span>
                <iconify-icon icon="mdi:package-variant" class="text-purple-400 text-lg"></iconify-icon>
            </div>
            <div class="text-2xl font-black text-white font-bold"><%= stats.totalProducts %></div>
            <div class="text-[10px] text-zinc-600 mt-1"><%= stats.activeProducts %> aktif</div>
        </div>
        <div class="stat-card fade-in" style="background:linear-gradient(135deg,rgba(16,185,129,.1),rgba(5,150,105,.05));border-color:rgba(16,185,129,.25);">
            <div class="flex items-center justify-between mb-2">
                <span class="text-[10px] text-zinc-500 font-semibold uppercase tracking-wide">Selesai</span>
                <iconify-icon icon="mdi:check-circle" class="text-emerald-400 text-lg"></iconify-icon>
            </div>
            <div class="text-2xl font-black text-white font-bold"><%= stats.doneTransactions %></div>
            <div class="text-[10px] text-emerald-600 mt-1">transaksi done</div>
        </div>
        <div class="stat-card fade-in" style="background:linear-gradient(135deg,rgba(234,179,8,.1),rgba(245,158,11,.05));border-color:rgba(234,179,8,.25);">
            <div class="flex items-center justify-between mb-2">
                <span class="text-[10px] text-zinc-500 font-semibold uppercase tracking-wide">Pending</span>
                <iconify-icon icon="mdi:clock-outline" class="text-yellow-400 text-lg"></iconify-icon>
            </div>
            <div class="text-2xl font-black text-white font-bold"><%= stats.pendingTransactions %></div>
            <div class="text-[10px] text-yellow-600 mt-1">menunggu</div>
        </div>
        <div class="stat-card fade-in" style="background:linear-gradient(135deg,rgba(22,163,74,.1),rgba(21,128,61,.05));border-color:rgba(22,163,74,.25);">
            <div class="flex items-center justify-between mb-2">
                <span class="text-[10px] text-zinc-500 font-semibold uppercase tracking-wide">Revenue</span>
                <iconify-icon icon="mdi:cash-multiple" class="text-green-400 text-lg"></iconify-icon>
            </div>
            <div class="text-lg font-black text-white font-bold">Rp <%= (stats.totalRevenue/1000).toFixed(0) %>K</div>
            <div class="text-[10px] text-green-600 mt-1">total pendapatan</div>
        </div>
        <div class="stat-card fade-in" style="background:linear-gradient(135deg,rgba(124,47,214,.1),rgba(124,47,214,.05));border-color:rgba(124,47,214,.25);">
            <div class="flex items-center justify-between mb-2">
                <span class="text-[10px] text-zinc-500 font-semibold uppercase tracking-wide">Users</span>
                <iconify-icon icon="mdi:account-group" class="text-purple-400 text-lg"></iconify-icon>
            </div>
            <div class="text-2xl font-black text-white font-bold"><%= stats.totalUsers %></div>
            <div class="text-[10px] text-purple-600 mt-1">member terdaftar</div>
        </div>
        <div class="stat-card fade-in" style="background:linear-gradient(135deg,rgba(234,88,12,.1),rgba(194,65,12,.05));border-color:rgba(234,88,12,.25);">
            <div class="flex items-center justify-between mb-2">
                <span class="text-[10px] text-zinc-500 font-semibold uppercase tracking-wide">Reseller</span>
                <iconify-icon icon="mdi:crown" class="text-orange-400 text-lg"></iconify-icon>
            </div>
            <div class="text-2xl font-black text-white font-bold"><%= stats.totalResellers || 0 %></div>
            <div class="text-[10px] text-orange-600 mt-1">reseller VIP aktif</div>
        </div>
    </div>

    <!-- GRAFIK PENJUALAN 7 HARI -->
    <div class="neon-card p-4 mb-5">
        <div class="section-label mb-3">Grafik Penjualan 7 Hari Terakhir</div>
        <div style="position:relative;height:180px;">
            <canvas id="salesChart"></canvas>
        </div>
    </div>

    <div class="section-label">Quick Actions</div>
    <div class="grid grid-cols-3 sm:grid-cols-6 gap-2 mb-5">
        <button onclick="switchTab('products')" class="flex flex-col items-center gap-1.5 p-3 rounded-xl bg-purple-500/10 border border-purple-500/20 hover:bg-purple-500/20 transition-all">
            <iconify-icon icon="mdi:package-variant" class="text-xl text-purple-400"></iconify-icon>
            <span class="text-[10px] font-semibold text-purple-300">Produk</span>
        </button>
        <button onclick="switchTab('transactions')" class="flex flex-col items-center gap-1.5 p-3 rounded-xl bg-emerald-500/10 border border-emerald-500/20 hover:bg-emerald-500/20 transition-all">
            <iconify-icon icon="mdi:receipt-text" class="text-xl text-emerald-400"></iconify-icon>
            <span class="text-[10px] font-semibold text-emerald-300">Transaksi</span>
        </button>
        <button onclick="switchTab('users')" class="flex flex-col items-center gap-1.5 p-3 rounded-xl bg-purple-500/10 border border-purple-500/20 hover:bg-purple-500/20 transition-all">
            <iconify-icon icon="mdi:account-group" class="text-xl text-purple-400"></iconify-icon>
            <span class="text-[10px] font-semibold text-purple-300">Users</span>
        </button>
        <button onclick="switchTab('testimonials')" class="flex flex-col items-center gap-1.5 p-3 rounded-xl bg-purple-500/10 border border-purple-500/20 hover:bg-purple-500/20 transition-all">
            <iconify-icon icon="mdi:comment-quote" class="text-xl text-purple-400"></iconify-icon>
            <span class="text-[10px] font-semibold text-purple-300">Testimoni</span>
        </button>
        <button onclick="switchTab('settings')" class="flex flex-col items-center gap-1.5 p-3 rounded-xl bg-amber-500/10 border border-amber-500/20 hover:bg-amber-500/20 transition-all">
            <iconify-icon icon="mdi:cog" class="text-xl text-amber-400"></iconify-icon>
            <span class="text-[10px] font-semibold text-amber-300">Setting</span>
        </button>
        <button onclick="openAddProductModal()" class="flex flex-col items-center gap-1.5 p-3 rounded-xl bg-pink-500/10 border border-pink-500/20 hover:bg-pink-500/20 transition-all">
            <iconify-icon icon="mdi:plus-circle" class="text-xl text-pink-400"></iconify-icon>
            <span class="text-[10px] font-semibold text-pink-300">+Produk</span>
        </button>
    </div>

    <div class="section-label">Transaksi Terbaru</div>
    <div class="neon-card overflow-hidden">
        <div class="overflow-x-auto">
            <table class="data-table">
                <thead>
                    <tr>
                        <th>Kode</th>
                        <th>Produk</th>
                        <th>Harga</th>
                        <th>Status</th>
                        <th>Waktu</th>
                    </tr>
                </thead>
                <tbody>
                    <% transactions.slice(0,8).forEach(t => { %>
                    <tr>
                        <td class="font-mono text-purple-400 text-[11px]"><%= t.code %></td>
                        <td class="text-white max-w-[120px] truncate text-xs"><%= t.productName %></td>
                        <td class="text-emerald-400 font-bold whitespace-nowrap text-xs">Rp <%= (t.price/1000).toFixed(0) %>K</td>
                        <td>
                            <% if(t.outOfStock && !t.key){ %><span class="badge badge-failed" title="Pembayaran masuk tapi key belum terkirim, perlu diproses manual">❌ Gagal (Stok Habis)</span>
                            <% }else if(t.status==='done'){ %><span class="badge badge-done">Done</span>
                            <% }else{ %><span class="badge badge-pending">Pending</span><% } %>
                        </td>
                        <td class="text-zinc-500 text-[11px] whitespace-nowrap"><%= t.time %></td>
                    </tr>
                    <% }) %>
                    <% if(!transactions.length){ %>
                    <tr><td colspan="5" class="text-center py-6 text-zinc-600 text-xs">Belum ada transaksi</td></tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- ── PRODUK ── -->
<div id="pane-products" class="tab-pane">
    <div class="flex items-center justify-between mb-2 gap-2">
        <div class="section-label mb-0" id="products-counter">Manajemen Produk (<%= productsTotalCount %>)</div>
        <div class="flex items-center gap-2">
            <button onclick="openAddProductModal()" class="btn-primary text-xs">
                <iconify-icon icon="mdi:plus"></iconify-icon> Tambah
            </button>
        </div>
    </div>
    <!-- Tombol "Set Auto Semua" (bulk-set-auto) sengaja DIHAPUS -- ini
         kemungkinan besar akar masalah insiden Sep 2026: sekali klik ubah
         SEMUA produk ke stockSource 'auto' (vipibmstore) tanpa peringatan,
         padahal kredensial vipibmstore gak pernah diisi. vipibmstore sudah
         dicabut total dari sistem; sumber stok sekarang cuma Manual atau
         GhostSeller, dan keduanya wajib diatur per-produk/per-varian secara
         sadar lewat Edit Produk, gak ada lagi jalan pintas bulk-ubah. -->
    <div class="relative mb-3">
        <iconify-icon icon="mdi:magnify" class="absolute left-3 top-1/2 -translate-y-1/2 text-zinc-600 text-sm"></iconify-icon>
        <input type="text" id="prodSearchInput" oninput="onProductSearchInput(this.value)" placeholder="Cari produk (nama/kategori)..."
            class="hm-input w-full !pl-9 text-xs" autocomplete="off">
    </div>
    <!-- FIX "ANTI-LAG": halaman awal cuma render <%= productsPage.length %> dari
    <%= productsTotalCount %> produk (lihat GET /admin di server.js). Sisanya
    dimuat lewat infinite-scroll (loadMoreProducts()) supaya jumlah produk
    yang bisa ditampung tidak dibatasi ukuran DOM/HTML awal. -->
    <div class="space-y-2.5" id="products-list">
        <% productsPage.forEach(p => {
            const pricing = p.pricingOptions || p.items?.map(i=>({days:parseInt((i.l.match(/(\d+)/)||[])[1]||0),price:i.p})) || [];
            const minPrice = pricing.length ? Math.min(...pricing.map(o=>o.price)) : 0;
            // FIX BUG "Harga Reseller/Key kelihatan kosong padahal produk
            // punya opsi harga": pricingJson di-escape MANUAL di sini (ganti
            // " jadi &quot;) supaya aman ditaruh sebagai value atribut HTML.
            // Tapi dulu dicetak pakai tag EJS versi ESCAPED (sama dengan
            // ganda =), yang otomatis nge-escape SEMUA output-nya lagi,
            // termasuk karakter "&". Akibatnya "&quot;" yang udah aman
            // ke-escape DOBEL jadi "&amp;quot;". Pas dibaca balik lewat JS
            // (dataset.pricing), hasilnya bukan tanda kutip " yang valid buat
            // JSON, tapi teks harfiah "&quot;" -- JSON.parse() gagal, jatuh
            // ke catch{} yang diam-diam balikin array kosong. Makanya modal
            // Reseller/Key kelihatan "produk ini belum punya opsi harga"
            // walau pricingOptions-nya sebenarnya ada isinya. Fix: escape
            // SEMUA karakter berbahaya HTML-attribute (&, ", <, >) sekali di
            // sini, lalu keluarkan pakai tag EJS versi RAW (tanda minus,
            // tidak di-escape lagi) di kedua tombol Key & Reseller di bawah.
            const pricingJson = JSON.stringify(pricing)
                .replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
        %>
        <div class="prod-card fade-in" data-product-id="<%= p.id %>">
            <div class="w-12 h-12 rounded-xl overflow-hidden flex-shrink-0 border border-white/10 bg-purple-500/10" id="prod-img-<%= p.id %>">
                <% if(p.image){ %>
                    <img src="<%= p.image %>" alt="<%= p.name %>" class="w-full h-full object-cover" loading="lazy"
                         onerror="this.style.display='none';this.parentNode.innerHTML='<div style=display:flex;align-items:center;justify-content:center;width:100%;height:100%><iconify-icon icon=\'mdi:gamepad-variant\' style=font-size:20px;color:#38bdf8></iconify-icon></div>'">
                <% }else{ %>
                    <div class="w-full h-full flex items-center justify-center">
                        <iconify-icon icon="mdi:gamepad-variant" class="text-xl text-purple-400"></iconify-icon>
                    </div>
                <% } %>
            </div>
            <div class="flex-1 min-w-0">
                <div class="flex items-center gap-2 mb-0.5 flex-wrap">
                    <span class="text-sm font-semibold text-white truncate"><%= p.name %></span>
                    <span class="badge <%= p.status==='active'?'badge-done':'badge-inactive' %>"><%= p.status==='active'?'Aktif':'Off' %></span>
                </div>
                <div class="flex items-center gap-2 text-[10px] text-zinc-500 flex-wrap">
                    <span class="px-1.5 py-0.5 rounded bg-purple-500/15 text-purple-400 font-bold uppercase"><%= p.category %></span>
                    <span class="prod-key-count <%= (p.keys?.length||0)>0?'text-emerald-400':'text-red-400' %>">
                        <iconify-icon icon="mdi:key" class="align-middle"></iconify-icon> <%= p.keys?.length||0 %> key
                    </span>
                    <span><%= p.sold||0 %> terjual</span>
                    <!-- CATATAN (belum diperbaiki, di luar scope fix label sekarang):
                    key tag "KEY:N" cuma nyimpen angka N tanpa unit. Kalau produk
                    punya 2 paket dengan angka durasi sama tapi unit beda (mis.
                    "1 Jam" & "1 Hari", keduanya days=1), badge di bawah dan sistem
                    stok TIDAK BISA membedakan key mana yang punya buat paket mana --
                    keduanya bakal kehitung dari key yang sama (":1"). Kalau butuh
                    dukungan penuh 2 paket beda unit dgn angka sama, skema tag key
                    perlu diperluas jadi "KEY:N:unit" (perubahan lebih besar, ganti
                    di banyak titik: sini, resolveStockSourceForDays, checkout, dll). -->
                    <span class="text-zinc-500 flex items-center gap-1 flex-wrap">
                        <% pricing.forEach(po => { const puLabel = po.unit === 'jam' ? 'j' : po.unit === 'menit' ? 'm' : 'h'; %>
                        <span class="px-1.5 py-0.5 rounded text-[9px] font-bold <%= (p.keys||[]).filter(k => k.endsWith(':'+po.days)).length > 0 ? 'text-emerald-400 bg-emerald-500/10' : 'text-zinc-600 bg-zinc-800/50' %>">
                            <%= po.days %><%= puLabel %>: <%= (p.keys||[]).filter(k => k.endsWith(':'+po.days)).length %>
                        </span>
                        <% }) %>
                        <% const genericCnt = (p.keys||[]).filter(k => !k.includes(':')).length; if (genericCnt > 0) { %>
                        <span class="px-1.5 py-0.5 rounded text-[9px] font-bold text-yellow-400 bg-yellow-500/10">G: <%= genericCnt %></span>
                        <% } %>
                    </span>
                </div>
                <% if(minPrice>0){ %>
                    <div class="text-[10px] text-purple-400 font-bold mt-0.5">Mulai Rp <%= minPrice.toLocaleString('id-ID') %></div>
                <% } %>
            </div>
            <div class="flex flex-col gap-1.5 flex-shrink-0">
                <a href="/admin/product-edit?id=<%= p.id %>" class="btn-warning py-1 px-2 text-[10px] text-center">
                    <iconify-icon icon="mdi:pencil"></iconify-icon> Edit
                </a>
                <button
                    data-pid="<%= p.id %>"
                    data-name="<%- (p.name||'').replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;') %>"
                    data-count="<%= p.keys?.length||0 %>"
                    data-pricing="<%- pricingJson %>"
                    onclick="openAddKeysModal(this.dataset.pid, this.dataset.name, parseInt(this.dataset.count), this.dataset.pricing)"
                    class="btn-success py-1 px-2 text-[10px]">
                    <iconify-icon icon="mdi:key-plus"></iconify-icon> Key
                </button>
                <button
                    data-pid="<%= p.id %>"
                    data-name="<%- (p.name||'').replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;') %>"
                    data-pricing="<%- pricingJson %>"
                    onclick="openResellerPriceModal(this.dataset.pid, this.dataset.name, this.dataset.pricing)"
                    class="py-1 px-2 text-[10px] font-bold rounded-lg flex items-center gap-1 justify-center"
                    style="background:rgba(234,179,8,.15);border:1px solid rgba(234,179,8,.35);color:#facc15">
                    <iconify-icon icon="mdi:crown"></iconify-icon> Reseller
                </button>
                <button onclick="deleteProduct('<%= p.id %>')" class="btn-danger py-1 px-2 text-[10px]">
                    <iconify-icon icon="mdi:trash-can"></iconify-icon>
                </button>
            </div>
        </div>
        <% }) %>
        <% if(!productsTotalCount){ %>
        <div class="text-center py-10 text-zinc-600 text-xs">Belum ada produk. <button onclick="openAddProductModal()" class="text-purple-400 underline">Tambah sekarang</button></div>
        <% } %>
    </div>
    <div id="products-search-empty" class="text-center py-8 text-zinc-600 text-xs" style="display:none">Tidak ada produk yang cocok.</div>
    <div class="flex justify-center mt-3">
        <button id="loadMoreProductsBtn" onclick="loadMoreProducts()" class="text-xs px-4 py-2 rounded-lg font-bold flex items-center gap-1.5"
            style="background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.08);color:#a1a1aa;<%= productsHasMore ? '' : 'display:none' %>">
            <iconify-icon icon="mdi:chevron-down"></iconify-icon> Muat Lebih Banyak
        </button>
    </div>
</div>

<!-- ── TRANSAKSI ── -->
<div id="pane-transactions" class="tab-pane">
    <div class="flex items-center justify-between mb-3 gap-2 flex-wrap">
        <div class="section-label mb-0">Semua Transaksi (<%= transactions.length %>)</div>
        <div class="flex gap-2">
            <button onclick="filterTrx('all')" id="trxf-all" class="trx-filter px-2.5 py-1 rounded-lg text-[10px] font-bold bg-purple-500/20 border border-indigo-500/40 text-purple-300">Semua</button>
            <button onclick="filterTrx('pending')" id="trxf-pending" class="trx-filter px-2.5 py-1 rounded-lg text-[10px] font-bold bg-white/3 border border-white/10 text-zinc-500">Pending</button>
            <button onclick="filterTrx('done')" id="trxf-done" class="trx-filter px-2.5 py-1 rounded-lg text-[10px] font-bold bg-white/3 border border-white/10 text-zinc-500">Done</button>
            <button onclick="filterTrx('failed')" id="trxf-failed" class="trx-filter px-2.5 py-1 rounded-lg text-[10px] font-bold bg-white/3 border border-white/10 text-zinc-500">❌ Gagal</button>
        </div>
    </div>
    <div class="relative mb-3">
        <iconify-icon icon="mdi:key-search" class="absolute left-3 top-1/2 -translate-y-1/2 text-zinc-600 text-sm"></iconify-icon>
        <input type="text" id="trxKeySearch" oninput="filterTrxByKey(this.value)" placeholder="Lacak Key... (cari key persis atau sebagian)"
            class="hm-input w-full !pl-9 text-xs" autocomplete="off">
    </div>
    <div class="space-y-2" id="trx-list">
        <% transactions.forEach(t => {
            // 'gacha' transactions (dari /arcade/play) punya bentuk data yang
            // beda sama sekali dari transaksi pembelian biasa — tidak ada
            // code/productName/customerName/wa/price/key, adanya
            // gameLabel/prizeLabel/wonProductName/wonKey. Tanpa normalisasi
            // ini, kartu gacha tampil kosong/Rp 0 di admin panel (kelihatan
            // kayak transaksi rusak, padahal cuma field yang beda nama).
            const isGacha = t.type === 'gacha';
            const gachaUser = isGacha ? users.find(u => u.id === t.userId) : null;
            const displayTitle = isGacha ? (t.gameLabel || 'Arcade') : t.productName;
            const displayCustomer = isGacha ? (gachaUser?.username || '-') : t.customerName;
            const displayWa = isGacha ? (gachaUser?.wa || '-') : t.wa;
            const displayKey = isGacha ? t.wonKey : t.key;
            // FIX: badge sebelumnya SELALU bilang "Stok Habis" walau
            // penyebab aslinya beda (mis. "Reseller API mengembalikan
            // status 525" = error koneksi/SSL ke provider, atau "Mapping
            // manual diperlukan" = produk belum di-mapping) -- membingungkan
            // admin karena dikira produk beneran kehabisan stok padahal
            // bukan. Tentukan label dari isi stockError.
            let failLabel = '❌ Gagal (Stok Habis)';
            if (t.outOfStock && !t.key && t.stockError) {
                const se = t.stockError;
                if (/status \d{3}|timeout|network|koneksi|ECONN|gagal menghubungi/i.test(se)) {
                    failLabel = '❌ Gagal (Error Koneksi API)';
                } else if (/mapping|tidak ditemukan produk provider/i.test(se)) {
                    failLabel = '❌ Gagal (Belum Dimapping)';
                } else if (/stok habis|tidak ada key tersedia/i.test(se)) {
                    failLabel = '❌ Gagal (Stok Habis)';
                } else {
                    failLabel = '❌ Gagal (Perlu Dicek)';
                }
            }
        %>
        <div class="neon-card p-3.5 fade-in trx-item" data-trx-id="<%= t.id %>" data-status="<%= t.status %>" data-outofstock="<%= (t.outOfStock && !t.key) ? 'failed' : '' %>" data-key="<%= (displayKey || '').toLowerCase() %>">
            <div class="flex items-start justify-between gap-2 mb-2">
                <div>
                    <div class="flex items-center gap-2">
                        <div class="text-[11px] font-mono font-bold text-purple-400"><%= t.code || (isGacha ? 'GACHA' : '-') %></div>
                        <% if(t.type==='reseller'){ %><span class="badge" style="background:rgba(234,179,8,.15);color:#fbbf24;border:1px solid rgba(234,179,8,.3)"><iconify-icon icon="mdi:crown" class="text-[9px]"></iconify-icon> Reseller</span><% } %>
                        <% if(isGacha){ %><span class="badge" style="background:rgba(168,85,247,.15);color:#c084fc;border:1px solid rgba(168,85,247,.3)"><iconify-icon icon="mdi:slot-machine" class="text-[9px]"></iconify-icon> Gacha</span><% } %>
                    </div>
                    <div class="text-sm font-semibold text-white mt-0.5"><%= displayTitle %></div>
                </div>
                <% if(t.outOfStock && !t.key){ %><span class="badge badge-failed" id="trx-status-badge-<%= t.id %>" title="Pembayaran sudah masuk tapi key belum terkirim, perlu diproses manual"><%= failLabel %></span>
                <% }else if(t.status==='done'){ %><span class="badge badge-done">Done</span>
                <% }else{ %><span class="badge badge-pending" id="trx-status-badge-<%= t.id %>">Pending</span><% } %>
            </div>
            <div class="grid grid-cols-2 gap-x-4 gap-y-1 mb-3">
                <div class="text-[10px] text-zinc-500">Customer: <span class="text-zinc-300"><%= displayCustomer %></span></div>
                <div class="text-[10px] text-zinc-500">WA: <span class="text-zinc-300"><%= displayWa %></span></div>
                <% if(isGacha){ %>
                <div class="text-[10px] text-zinc-500 col-span-2">Hadiah: <span class="text-purple-300 font-bold"><%= t.prizeLabel %></span></div>
                <% } else { %>
                <div class="text-[10px] text-zinc-500">Harga: <span class="text-emerald-400 font-bold">Rp <%= (t.price || 0).toLocaleString('id-ID') %></span></div>
                <% } %>
                <div class="text-[10px] text-zinc-500">Waktu: <span class="text-zinc-300"><%= t.time %></span></div>
                <% if(displayKey){ %>
                <div class="text-[10px] text-zinc-500 col-span-2 flex items-center gap-1.5">
                    Key: <span class="text-emerald-400 font-mono font-bold key-value" data-real="<%= displayKey %>" data-masked="true">••••••••</span>
                    <button type="button" onclick="toggleKeyVisibility(this)" class="text-zinc-500 hover:text-zinc-300 flex-shrink-0" title="Tampilkan/sembunyikan key">
                        <iconify-icon icon="mdi:eye-outline" style="font-size:13px"></iconify-icon>
                    </button>
                </div>
                <% } %>
                <% if(t.outOfStock && !t.key && t.stockError){ %>
                <div class="text-[10px] text-red-400 col-span-2">⚠️ <%= t.stockError %></div>
                <% } %>
                <% if(t.isStatic && t.fallbackReason){ %>
                <div class="text-[10px] text-amber-400 col-span-2" title="<%= t.fallbackReason %>">
                    <iconify-icon icon="mdi:alert-circle-outline"></iconify-icon> QRIS otomatis gagal saat order dibuat, jatuh ke manual: <%= t.fallbackReason.slice(0,80) %>
                </div>
                <% } %>
            </div>
            <div class="flex gap-2">
                <% if(t.status !== 'done' || (t.outOfStock && !t.key)){ %>
                <button onclick="confirmTransaction('<%= t.id %>')" class="btn-success py-1 px-3 text-[10px]" id="trx-confirm-<%= t.id %>">
                    <iconify-icon icon="<%= (t.outOfStock && !t.key) ? 'mdi:refresh' : 'mdi:check-circle' %>"></iconify-icon> <%= (t.outOfStock && !t.key) ? 'Coba Lagi' : 'Konfirmasi Bayar' %>
                </button>
                <% } %>
                <button onclick="deleteTransaction('<%= t.id %>')" class="btn-danger py-1 px-3 text-[10px]">
                    <iconify-icon icon="mdi:trash-can"></iconify-icon>
                </button>
            </div>
        </div>
        <% }) %>
        <% if(!transactions.length){ %>
        <div class="text-center py-10 text-zinc-600 text-xs">Belum ada transaksi</div>
        <% } %>
    </div>
</div>

<!-- ── USERS ── -->
<div id="pane-users" class="tab-pane">
    <div class="flex items-center justify-between mb-3">
        <div class="section-label mb-0">Manajemen Users (<%= users.length %>)</div>
        <div class="text-[10px] text-zinc-600"><%= users.filter(u=>u.is_reseller).length %> reseller aktif</div>
    </div>
    <!-- Search user -->
    <div class="relative mb-3">
        <iconify-icon icon="mdi:magnify" class="absolute left-3 top-1/2 -translate-y-1/2 text-zinc-500 pointer-events-none"></iconify-icon>
        <input type="text" id="userSearchInput" oninput="filterUsers(this.value)"
            placeholder="Cari username, nomor HP, atau Gmail..."
            class="hm-input pl-8 w-full text-xs">
    </div>
    <div class="space-y-2" id="usersListContainer">
        <% users.forEach(u => {
            // FIX KEAMANAN: u.username ditempel APA ADANYA di beberapa
            // onclick="fn('...','...')" di bawah. Kalau username (terutama
            // akun lama dari sebelum validasi registrasi diperketat)
            // mengandung tanda kutip satu/dua, backslash, atau simbol HTML,
            // itu bisa memutus syntax JS di tombolnya atau nyuntik kode
            // yang jalan otomatis di browser admin. Escape dulu untuk
            // konteks string-JS-di-dalam-atribut-HTML: backslash duluan
            // (biar escape char lain nggak keganda), lalu kutip satu
            // (batas string JS), lalu kutip dua/</>/& (batas atribut HTML).
            const safeUsername = String(u.username || '')
                .replace(/\\/g, '\\\\').replace(/'/g, "\\'")
                .replace(/&/g, '&amp;').replace(/"/g, '&quot;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
        %>
        <div class="neon-card p-3.5 flex items-center gap-3 fade-in user-card"
             data-user-id="<%= u.id %>"
             data-search="<%= (u.username||'').toLowerCase() %> <%= (u.wa||'').toLowerCase() %> <%= (u.email||'').toLowerCase() %>">
            <div class="relative flex-shrink-0">
                <% if(u.photo){ %>
                    <img src="<%= u.photo %>" class="w-10 h-10 rounded-full object-cover border border-white/10">
                <% } else { %>
                    <div class="w-10 h-10 rounded-full flex items-center justify-center text-white font-black text-sm"
                         style="background:linear-gradient(135deg,#6366f1,#6366f1)">
                        <%= u.username.charAt(0).toUpperCase() %>
                    </div>
                <% } %>
                <% if(u.is_reseller){ %>
                    <div class="absolute -top-1 -right-1 w-4 h-4 rounded-full flex items-center justify-center" style="background:#fbbf24">
                        <iconify-icon icon="mdi:crown" class="text-[8px] text-black"></iconify-icon>
                    </div>
                <% } %>
            </div>
            <div class="flex-1 min-w-0">
                <div class="flex items-center gap-2">
                    <div class="text-sm font-semibold text-white truncate"><%= u.username %></div>
                    <% if(u.is_reseller){ %>
                        <span class="badge" style="background:rgba(234,179,8,.15);color:#fbbf24;border:1px solid rgba(234,179,8,.3);font-size:9px">VIP</span>
                    <% } %>
                </div>
                <div class="text-[10px] text-zinc-500">WA: <%= u.wa %></div>
                <% if(u.email){ %><div class="text-[10px] text-zinc-500">Email: <%= u.email %></div><% } %>
                <% if(u.is_reseller && u.reseller_code){ %>
                    <div class="text-[10px] font-mono text-yellow-500"><%= u.reseller_code %></div>
                <% } else { %>
                    <div class="text-[10px] text-zinc-600">Bergabung: <%= new Date(u.createdAt).toLocaleDateString('id-ID') %></div>
                <% } %>
                <% if(u.is_reseller){ %>
                    <div class="text-[10px] font-bold text-emerald-400 mt-0.5">Saldo: Rp <%= (u.balance||0).toLocaleString('id-ID') %></div>
                <% } %>
            </div>
            <div class="flex flex-col gap-1.5 flex-shrink-0">
                <button onclick="toggleReseller('<%= u.id %>','<%- safeUsername %>')"
                    class="py-1 px-2 text-[10px] rounded-lg border transition-all font-semibold <%= u.is_reseller ? 'bg-yellow-500/15 border-yellow-500/30 text-yellow-400 hover:bg-yellow-500/25' : 'bg-white/5 border-white/15 text-zinc-400 hover:bg-purple-500/15 hover:text-purple-400 hover:border-purple-500/30' %>"
                    id="reseller-btn-<%= u.id %>">
                    <iconify-icon icon="<%= u.is_reseller ? 'mdi:crown-off' : 'mdi:crown' %>"></iconify-icon>
                    <%= u.is_reseller ? 'Cabut' : 'Jadikan' %>
                </button>
                <% if(u.is_reseller){ %>
                <button onclick="adjustBalance('<%= u.id %>','<%- safeUsername %>')"
                    class="py-1 px-2 text-[10px] rounded-lg border transition-all font-semibold bg-emerald-500/10 border-emerald-500/25 text-emerald-400 hover:bg-emerald-500/20">
                    <iconify-icon icon="mdi:wallet-plus"></iconify-icon> Saldo
                </button>
                <% } %>
                <button onclick="deleteUser('<%= u.id %>','<%- safeUsername %>')" class="btn-danger py-1 px-2 text-[10px]">
                    <iconify-icon icon="mdi:trash-can"></iconify-icon>
                </button>
            </div>
        </div>
        <% }) %>
        <% if(!users.length){ %>
        <div class="text-center py-10 text-zinc-600 text-xs">Belum ada user</div>
        <% } %>
    </div>
    <div id="userSearchEmpty" class="hidden text-center py-6 text-zinc-600 text-xs">Tidak ada user yang cocok</div>
</div>
</div>

<!-- ── TESTIMONI ── -->
<div id="pane-testimonials" class="tab-pane">
    <div class="flex items-center justify-between mb-4">
        <div class="section-label mb-0">Testimoni</div>
        <button onclick="openModal('modal-testi')" class="btn-primary text-xs">
            <iconify-icon icon="mdi:plus"></iconify-icon> Tambah
        </button>
    </div>
    <div id="testi-list" class="space-y-2">
        <div class="text-center py-8 text-zinc-600">
            <iconify-icon icon="mdi:loading" class="text-2xl animate-spin block mx-auto mb-2 text-purple-400"></iconify-icon>
            <p class="text-xs">Memuat...</p>
        </div>
    </div>
</div>

<!-- ── KEY POOL ── -->
<div id="pane-keys" class="tab-pane">
    <div class="flex items-center justify-between mb-4">
        <div class="section-label mb-0">Manajemen Key Lisensi</div>
        <span class="text-[10px] text-zinc-600" id="keyStatsLabel">—</span>
    </div>

    <!-- Form tambah / generate key -->
    <div class="neon-card p-4 mb-4">
        <div class="text-xs font-bold text-purple-400 mb-3 flex items-center gap-2">
            <iconify-icon icon="mdi:key-plus" class="text-base"></iconify-icon> Tambah Key Manual
        </div>
        <div class="grid grid-cols-2 gap-2 mb-2">
            <div>
                <div class="setting-row-label">Kode Key</div>
                <input type="text" id="newKeyCode" class="hm-input font-mono uppercase" placeholder="HERO-001" maxlength="20" oninput="this.value=this.value.toUpperCase()">
            </div>
            <div>
                <div class="setting-row-label">Durasi (Hari)</div>
                <input type="number" id="newKeyDuration" class="hm-input" placeholder="30" min="1" max="9999">
            </div>
        </div>
        <div class="mb-2">
            <div class="setting-row-label">Label (opsional)</div>
            <input type="text" id="newKeyLabel" class="hm-input" placeholder="30 Hari Akses Premium">
        </div>
        <div class="mb-3">
            <div class="setting-row-label">Catatan (opsional)</div>
            <input type="text" id="newKeyNote" class="hm-input" placeholder="Untuk pelanggan paket...">
        </div>
        <button onclick="addKey()" class="btn-primary w-full">
            <iconify-icon icon="mdi:plus-circle"></iconify-icon> Tambah Key
        </button>
    </div>

    <!-- Generate otomatis -->
    <div class="neon-card p-4 mb-4">
        <div class="text-xs font-bold text-purple-400 mb-3 flex items-center gap-2">
            <iconify-icon icon="mdi:auto-fix" class="text-base"></iconify-icon> Generate Key Otomatis
        </div>
        <div class="grid grid-cols-3 gap-2 mb-2">
            <div>
                <div class="setting-row-label">Jumlah</div>
                <input type="number" id="genCount" class="hm-input" placeholder="5" min="1" max="100" value="5">
            </div>
            <div>
                <div class="setting-row-label">Durasi (Hari)</div>
                <input type="number" id="genDuration" class="hm-input" placeholder="30" min="1" value="30">
            </div>
            <div>
                <div class="setting-row-label">Prefix</div>
                <input type="text" id="genPrefix" class="hm-input uppercase" placeholder="KEY" maxlength="8" value="KEY" oninput="this.value=this.value.toUpperCase()">
            </div>
        </div>
        <button onclick="generateKeys()" class="btn-primary w-full" style="background:linear-gradient(135deg,#7c2fd6,#a855f7)">
            <iconify-icon icon="mdi:lightning-bolt"></iconify-icon> Generate Keys
        </button>
        <div id="genResult" class="mt-2 hidden"></div>
    </div>

    <!-- Daftar key -->
    <div class="section-label">Daftar Key (<span id="keyTotal">0</span>)</div>
    <div class="space-y-2" id="keysList">
        <div class="text-center py-6 text-zinc-600 text-xs">
            <iconify-icon icon="mdi:loading" class="text-xl text-purple-400 animate-spin block mx-auto mb-2"></iconify-icon>
            Memuat...
        </div>
    </div>
</div>

<!-- ── VOUCHERS ── -->
<div id="pane-vouchers" class="tab-pane">
    <div class="flex items-center justify-between mb-4">
        <div class="section-label mb-0">Manajemen Voucher Diskon</div>
        <span class="text-[10px] text-zinc-600" id="voucherStatsLabel">—</span>
    </div>

    <!-- Form tambah voucher -->
    <div class="neon-card p-4 mb-4">
        <div class="text-xs font-bold text-green-400 mb-3 flex items-center gap-2">
            <iconify-icon icon="mdi:ticket-percent" class="text-base"></iconify-icon> Tambah Voucher
        </div>
        <div class="grid grid-cols-2 sm:grid-cols-4 gap-2 mb-2">
            <div>
                <div class="setting-row-label">Kode Voucher</div>
                <input type="text" id="newVoucherCode" class="hm-input font-mono uppercase" placeholder="DISC30" maxlength="20" oninput="this.value=this.value.toUpperCase()">
            </div>
            <div>
                <div class="setting-row-label">Tipe</div>
                <select id="newVoucherType" class="hm-input">
                    <option value="percent">Persen (%)</option>
                    <option value="nominal">Nominal (Rp)</option>
                </select>
            </div>
            <div>
                <div class="setting-row-label">Nilai</div>
                <input type="number" id="newVoucherValue" class="hm-input" placeholder="10 / 5000" min="1">
            </div>
            <div>
                <div class="setting-row-label">Min. Belanja</div>
                <input type="number" id="newVoucherMin" class="hm-input" placeholder="0 (tanpa min)" min="0" value="0">
            </div>
        </div>
        <div class="grid grid-cols-2 sm:grid-cols-3 gap-2 mb-2">
            <div>
                <div class="setting-row-label">Maks. Pemakaian</div>
                <input type="number" id="newVoucherMax" class="hm-input" placeholder="0 = tak terbatas" min="0" value="0">
            </div>
            <div>
                <div class="setting-row-label">Per User</div>
                <input type="number" id="newVoucherPerUser" class="hm-input" placeholder="1" min="1" value="1">
            </div>
            <div>
                <div class="setting-row-label">Kadaluarsa</div>
                <input type="date" id="newVoucherExpiry" class="hm-input">
            </div>
        </div>
        <div class="mb-3">
            <div class="setting-row-label">Deskripsi (opsional)</div>
            <input type="text" id="newVoucherDesc" class="hm-input" placeholder="Diskon 30% untuk semua produk">
        </div>
        <div class="mb-3 flex items-center gap-3 p-3 rounded-xl" style="background:rgba(234,179,8,.06);border:1px solid rgba(234,179,8,.18);">
            <input type="checkbox" id="newVoucherExcludeReseller" class="w-4 h-4 rounded accent-yellow-400">
            <div>
                <div class="text-[11px] font-semibold text-yellow-300">Tidak berlaku untuk Reseller</div>
                <div class="text-[10px] text-zinc-500">Cegah double-discount: reseller tidak bisa pakai voucher ini karena sudah dapat harga reseller</div>
            </div>
        </div>
        <button onclick="addVoucher()" class="btn-primary w-full" style="background:linear-gradient(135deg,#059669,#10b981)">
            <iconify-icon icon="mdi:plus-circle"></iconify-icon> Buat Voucher
        </button>
    </div>

    <!-- Daftar voucher -->
    <div class="space-y-2" id="vouchersList">
        <div class="text-center py-6 text-zinc-600 text-xs">
            <iconify-icon icon="mdi:loading" class="text-xl text-purple-400 animate-spin block mx-auto mb-2"></iconify-icon>
            Memuat...
        </div>
    </div>
</div>

<!-- ── ARCADE (Gacha Coin Game) ── -->
<div id="pane-arcade" class="tab-pane">
    <div class="section-label mb-3">Kelola Arcade Center</div>

    <!-- Beri koin ke user pilihan -->
    <div class="neon-card p-4 mb-5">
        <h3 class="text-xs font-bold text-white mb-1 flex items-center gap-2">
            <iconify-icon icon="mdi:poker-chip" class="text-amber-400"></iconify-icon> Beri Koin ke User
        </h3>
        <p class="text-[10px] text-zinc-500 mb-3">Pilih user, isi jumlah koin (boleh negatif untuk mengurangi), lalu simpan.</p>
        <div class="grid grid-cols-1 sm:grid-cols-[1fr_120px_auto] gap-2">
            <select id="coinUserSelect" class="hm-input text-xs">
                <option value="">— Pilih User —</option>
                <% users.forEach(u => { %>
                <option value="<%= u.id %>" data-username="<%= u.username %>"><%= u.username %> <% if(u.is_reseller){ %>(VIP)<% } %> — <%= (u.coins||0) %> koin</option>
                <% }) %>
            </select>
            <input type="number" id="coinAmountInput" class="hm-input text-xs" placeholder="Contoh: 5">
            <button onclick="giveCoinToUser()" class="btn-primary text-xs px-4 py-2 rounded-lg font-bold whitespace-nowrap">
                <iconify-icon icon="mdi:send"></iconify-icon> Kirim Koin
            </button>
        </div>
    </div>

    <!-- Konfigurasi hadiah gacha -->
    <div class="neon-card p-4">
        <h3 class="text-xs font-bold text-white mb-1 flex items-center gap-2">
            <iconify-icon icon="mdi:tune-variant" class="text-sky-400"></iconify-icon> Konfigurasi Hadiah
        </h3>
        <p class="text-[10px] text-zinc-500 mb-3">
            <strong>Bobot (weight)</strong> menentukan peluang relatif hadiah keluar — makin besar angkanya, makin sering keluar. <strong>Nilai</strong> adalah nominal saldo (Rp) atau persen diskon voucher.
        </p>
        <div class="overflow-x-auto">
        <table class="data-table" id="prizeConfigTable">
            <thead>
                <tr>
                    <th>Hadiah</th>
                    <th>Tipe</th>
                    <th style="width:110px">Bobot</th>
                    <th style="width:140px">Nilai / Product</th>
                    <th style="width:50px"></th>
                </tr>
            </thead>
            <tbody>
                <% gachaPrizes.forEach(p => { %>
                <tr data-prize-id="<%= p.id %>" data-prize-type="<%= p.type %>" data-product-id="<%= p.productId || '' %>">
                    <td class="text-zinc-300"><%= p.label %></td>
                    <td>
                        <span class="text-[10px] font-bold px-2 py-0.5 rounded"
                              style="background:<%= p.type==='zonk'?'rgba(148,163,184,.12);color:#94a3b8':(p.type==='saldo'?'rgba(52,211,153,.12);color:#34d399':(p.type==='key'?'rgba(250,204,21,.12);color:#facc15':'rgba(124,47,214,.12);color:#38bdf8')) %>">
                            <%= p.type==='zonk'?'Zonk':(p.type==='saldo'?'Saldo':(p.type==='key'?'Free Key':'Voucher %')) %>
                        </span>
                    </td>
                    <td><input type="number" min="0" class="hm-input text-xs prize-weight" value="<%= p.weight %>"></td>
                    <td>
                        <% if (p.type === 'zonk') { %>
                            <span class="text-zinc-600 text-[10px]">—</span>
                        <% } else if (p.type === 'key') { %>
                            <select class="hm-input text-xs prize-product">
                                <% products.forEach(prod => { %>
                                <option value="<%= prod.id %>" <%= prod.id === p.productId ? 'selected' : '' %>><%= prod.name %> (<%= (prod.keys||[]).length %> key)</option>
                                <% }) %>
                            </select>
                        <% } else { %>
                            <input type="number" min="0" class="hm-input text-xs prize-value" value="<%= p.value %>">
                        <% } %>
                    </td>
                    <td>
                        <% if (p.type !== 'zonk') { %>
                        <button onclick="this.closest('tr').remove()" class="text-red-400 hover:text-red-300" title="Hapus hadiah ini">
                            <iconify-icon icon="mdi:trash-can-outline"></iconify-icon>
                        </button>
                        <% } %>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>
        </div>
        <button onclick="addKeyPrizeRow()" class="btn-secondary text-xs px-4 py-2 rounded-lg font-bold mt-3">
            <iconify-icon icon="mdi:key-plus"></iconify-icon> Tambah Hadiah Key
        </button>
        <button onclick="saveArcadePrizes()" class="btn-primary text-xs px-4 py-2 rounded-lg font-bold mt-3">
            <iconify-icon icon="mdi:content-save"></iconify-icon> Simpan Pengaturan Arcade
        </button>
    </div>
</div>

<!-- ── SETTINGS ── -->
<div id="pane-settings" class="tab-pane">
    <div class="section-label mb-3">Pengaturan Situs</div>

    <!-- Status Database Supabase -->
    <div id="dbStatusCard" class="neon-card p-4 mb-3">
        <div class="text-xs font-bold text-emerald-400 mb-3 flex items-center gap-2">
            <iconify-icon icon="mdi:database"></iconify-icon> Status Database (Supabase)
        </div>
        <div id="dbStatusContent" class="text-xs text-zinc-400">Memeriksa koneksi...</div>
    </div>

    <!-- Info Situs -->
    <div class="neon-card p-5 mb-3">
        <div class="text-xs font-bold text-purple-400 mb-4 flex items-center gap-2">
            <iconify-icon icon="mdi:information-outline"></iconify-icon> Informasi Situs
        </div>
        <form id="settingsForm" class="space-y-3">
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                <div>
                    <div class="setting-row-label">Nama Situs</div>
                    <input type="text" name="siteName" value="<%= settings.siteName %>" class="hm-input">
                </div>
                <div>
                    <div class="setting-row-label">Nama Game Panel</div>
                    <input type="text" name="gamePanelName" value="<%= settings.gamePanelName %>" class="hm-input">
                </div>
            </div>
            <div>
                <div class="setting-row-label">Deskripsi / About</div>
                <textarea name="about" rows="3" class="hm-input"><%= settings.about %></textarea>
            </div>
            <div>
                <div class="setting-row-label">Marquee Text (teks berjalan)</div>
                <input type="text" name="marqueeText" value="<%= settings.marqueeText %>" class="hm-input">
            </div>
            <div>
                <div class="setting-row-label">URL Logo (gambar navbar)</div>
                <input type="text" name="logoUrl" value="<%= settings.logoUrl||'' %>" class="hm-input" placeholder="https://... atau /uploads/logo.png">
                <% if (settings.logoUrl) { %>
                <div class="mt-1.5 flex items-center gap-2">
                    <img src="<%= settings.logoUrl %>" alt="Logo preview" style="height:32px;max-width:120px;object-fit:contain;border-radius:4px;background:rgba(255,255,255,.05);padding:2px 6px;">
                    <span class="text-[10px]" style="color:var(--text-3);">Preview logo</span>
                </div>
                <% } %>
            </div>
            <div class="pt-2 border-t border-white/5">
                <div class="text-xs font-bold text-purple-400 mb-3 flex items-center gap-2">
                    <iconify-icon icon="mdi:phone"></iconify-icon> Kontak
                </div>
                <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                    <div>
                        <div class="setting-row-label">WhatsApp</div>
                        <input type="text" name="whatsapp" value="<%= settings.contact?.whatsapp||'' %>" class="hm-input" placeholder="628xxxxxxxxx">
                    </div>
                    <div>
                        <div class="setting-row-label">Telegram</div>
                        <input type="text" name="telegram" value="<%= settings.contact?.telegram||'' %>" class="hm-input" placeholder="username">
                    </div>
                    <div>
                        <div class="setting-row-label">Email</div>
                        <input type="email" name="email" value="<%= settings.contact?.email||'' %>" class="hm-input">
                    </div>
                </div>
                <div class="mt-3">
                    <div class="setting-row-label">Link Download APK / Channel</div>
                    <input type="url" name="downloadUrl" value="<%= settings.contact?.downloadUrl||'' %>" class="hm-input" placeholder="https://whatsapp.com/channel/... atau link Google Drive APK">
                    <div class="text-[10px] mt-1" style="color:var(--text-3);">Ditampilkan sebagai tombol "Download APK" di halaman produk. Kosongkan untuk menyembunyikan tombol.</div>
                </div>
                <div class="mt-3">
                    <div class="setting-row-label">Fonnte API Token (notif WA otomatis ke admin saat stok habis)</div>
                    <input type="text" name="fonnteToken" value="<%= settings.fonnteToken||'' %>" class="hm-input" placeholder="Isi token dari fonnte.com (opsional)">
                    <div class="text-[10px] mt-1" style="color:var(--text-3);">Kosongkan jika tidak digunakan. Daftar gratis di <a href="https://fonnte.com" target="_blank" class="text-purple-400 underline">fonnte.com</a>, hubungkan WA, lalu copy token-nya ke sini.</div>
                </div>

                <div class="flex items-center justify-between p-3 rounded-xl" style="background:rgba(124,47,214,.06);border:1px solid rgba(124,47,214,.15)">
                    <div class="pr-3">
                        <div class="text-xs font-semibold text-white">Batasi Login Admin ke 1 Perangkat</div>
                        <div class="text-[10px] text-zinc-500 mt-0.5">Kalau aktif, admin cuma bisa login di 1 perangkat dalam satu waktu — perangkat lain otomatis logout. Default: mati (boleh login di banyak perangkat sekaligus).</div>
                    </div>
                    <label class="relative inline-flex items-center cursor-pointer flex-shrink-0">
                        <input type="checkbox" name="singleDeviceAdminLogin" id="singleDeviceAdminLoginToggle" class="sr-only peer" <%= (settings.singleDeviceAdminLogin === true) ? 'checked' : '' %>>
                        <div class="w-9 h-5 bg-zinc-700 peer-focus:ring-2 peer-focus:ring-purple-500/30 rounded-full peer peer-checked:bg-purple-500 transition-all after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:rounded-full after:h-4 after:w-4 after:transition-all peer-checked:after:translate-x-4"></div>
                    </label>
                </div>

                <div class="flex items-center justify-between p-3 rounded-xl mt-3" style="background:rgba(124,47,214,.06);border:1px solid rgba(124,47,214,.15)">
                    <div class="pr-3">
                        <div class="text-xs font-semibold text-white">Tampilkan Section "Produk Populer"</div>
                        <div class="text-[10px] text-zinc-500 mt-0.5">Matikan kalau gak mau ada section "Produk Populer" (carousel geser) di halaman utama. Default: nyala.</div>
                    </div>
                    <label class="relative inline-flex items-center cursor-pointer flex-shrink-0">
                        <input type="checkbox" name="showPopularSection" id="showPopularSectionToggle" class="sr-only peer" <%= (settings.showPopularSection !== false) ? 'checked' : '' %>>
                        <div class="w-9 h-5 bg-zinc-700 peer-focus:ring-2 peer-focus:ring-purple-500/30 rounded-full peer peer-checked:bg-purple-500 transition-all after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:rounded-full after:h-4 after:w-4 after:transition-all peer-checked:after:translate-x-4"></div>
                    </label>
                </div>

                <div class="p-3 rounded-xl mt-3" style="background:rgba(124,47,214,.06);border:1px solid rgba(124,47,214,.15)">
                    <div class="text-xs font-semibold text-white">Jumlah Produk di Halaman Utama</div>
                    <div class="text-[10px] text-zinc-500 mt-0.5 mb-2">Batas produk yang ditampilkan di grid halaman utama biar gak lag kalau produk sudah banyak. Sisanya bisa dilihat user di halaman "Lihat Produk Lengkap" (juga jadi jumlah produk per halaman di sana). Default: 10.</div>
                    <input type="number" name="homeProductsLimit" value="<%= settings.homeProductsLimit || 10 %>" min="1" max="100" class="hm-input" placeholder="10">
                </div>
            </div>
            <button type="submit" class="btn-primary w-full justify-center py-3 text-sm">
                <iconify-icon icon="mdi:content-save"></iconify-icon> Simpan Pengaturan
            </button>
        </form>
    </div>

    <!-- Kategori -->
    <div class="neon-card p-5 mb-3">
        <div class="text-xs font-bold text-purple-400 mb-4 flex items-center gap-2">
            <iconify-icon icon="mdi:tag-multiple"></iconify-icon> Kategori Produk
        </div>
        <form id="categoryForm" class="space-y-3">
            <div>
                <div class="setting-row-label">Daftar kategori (satu per baris, format: id:Label)</div>
                <textarea name="categories" rows="6" class="hm-input font-mono text-xs"><% (settings.categories||[]).forEach(cat => { %><%= cat %>:<%= (settings.categoryLabels||{})[cat]||cat %>
<% }) %></textarea>
                <p class="text-[10px] text-zinc-600 mt-1">Contoh: freefire:FREE FIRE</p>
            </div>
            <button type="submit" class="btn-primary w-full justify-center py-2.5 text-sm">
                <iconify-icon icon="mdi:content-save"></iconify-icon> Simpan Kategori
            </button>
        </form>
    </div>

    <!-- BANNER CAROUSEL -->
    <div class="neon-card p-5 mb-3">
        <div class="text-xs font-bold text-purple-400 mb-4 flex items-center gap-2">
            <iconify-icon icon="mdi:image-multiple"></iconify-icon> Banner Carousel
        </div>

        <!-- Form tambah banner -->
        <form id="bannerAddForm" enctype="multipart/form-data" class="space-y-3 mb-5" onsubmit="addBanner(event)">
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                <div>
                    <div class="setting-row-label">Judul Banner (opsional)</div>
                    <input type="text" name="title" class="hm-input" placeholder="Contoh: PROMO SPESIAL">
                </div>
                <div>
                    <div class="setting-row-label">Subjudul (opsional)</div>
                    <input type="text" name="subtitle" class="hm-input" placeholder="Contoh: Diskon 20% hari ini">
                </div>
            </div>
            <div>
                <div class="setting-row-label">Link klik banner (opsional)</div>
                <input type="text" name="link" class="hm-input" placeholder="Contoh: /buy/xxx atau https://...">
            </div>
            <div>
                <div class="setting-row-label">Gambar Banner</div>
                <input type="file" name="bannerImg" accept="image/jpeg,image/png,image/webp" class="hm-input text-xs py-2" onchange="previewBannerFile(event)">
                <div id="bannerFilePreview" class="mt-2 hidden">
                    <img id="bannerFileImg" src="" class="w-full h-28 object-cover rounded-xl border border-indigo-500/25">
                </div>
            </div>
            <div>
                <div class="setting-row-label">Atau URL gambar</div>
                <input type="text" name="imageUrl" class="hm-input" placeholder="https://..." oninput="previewBannerUrl(this.value)" id="bannerUrlInput">
            </div>
            <div id="bannerUrlPreview" class="hidden">
                <img id="bannerUrlImg" src="" class="w-full h-28 object-cover rounded-xl border border-indigo-500/25">
            </div>
            <button type="submit" id="bannerAddBtn" class="btn-primary w-full justify-center py-2.5 text-sm">
                <iconify-icon icon="mdi:plus-circle"></iconify-icon> Tambah Banner
            </button>
        </form>

        <!-- Daftar banner -->
        <div class="setting-row-label mb-3">Banner Aktif</div>
        <div id="bannerList" class="space-y-2">
            <div class="text-xs text-zinc-600 text-center py-3">Memuat...</div>
        </div>
    </div>

    <!-- QRIS Payment -->
    <div class="neon-card p-5 mb-3">
        <div class="text-xs font-bold text-purple-400 mb-4 flex items-center gap-2">
            <iconify-icon icon="mdi:qrcode-scan"></iconify-icon> Pengaturan QRIS
        </div>
        <p class="text-[11px] text-zinc-500 mb-3">Pilih salah satu metode pembayaran yang aktif. Hanya satu metode yang bisa aktif dalam satu waktu.</p>

        <!-- Mode Selector -->
        <div class="grid grid-cols-3 gap-2 mb-4">
            <button type="button" id="modeStaticBtn" onclick="setQrisMode('static')"
                class="py-2.5 rounded-xl text-[11px] font-bold border transition-all flex flex-col items-center justify-center gap-1
                <%= (settings.qrisMode||'static')==='static' ? 'bg-purple-500/20 border-indigo-500/50 text-purple-300' : 'bg-white/3 border-white/10 text-zinc-500' %>">
                <iconify-icon icon="mdi:image" class="text-base"></iconify-icon> Statis
            </button>
            <button type="button" id="modePakasirBtn" onclick="setQrisMode('pakasir')"
                class="py-2.5 rounded-xl text-[11px] font-bold border transition-all flex flex-col items-center justify-center gap-1
                <%= (settings.qrisMode||'static')==='pakasir' ? 'bg-purple-500/20 border-indigo-500/50 text-purple-300' : 'bg-white/3 border-white/10 text-zinc-500' %>">
                <iconify-icon icon="mdi:api" class="text-base"></iconify-icon> Pakasir
            </button>
            <button type="button" id="modeGenspayBtn" onclick="setQrisMode('genspay')"
                class="py-2.5 rounded-xl text-[11px] font-bold border transition-all flex flex-col items-center justify-center gap-1
                <%= (settings.qrisMode||'static')==='genspay' ? 'bg-purple-500/20 border-indigo-500/50 text-purple-300' : 'bg-white/3 border-white/10 text-zinc-500' %>">
                <iconify-icon icon="mdi:api" class="text-base"></iconify-icon> GensPay
            </button>
        </div>

        <!-- QRIS Statis -->
        <div id="sectionStatic" class="<%= (settings.qrisMode||'static')==='static' ? '' : 'hidden' %>">
            <p class="text-[11px] text-zinc-500 mb-3">Upload foto QRIS dari rekening kamu (GoPay, DANA, OVO, atau QRIS Merchant). Pembayaran dikonfirmasi manual oleh admin.</p>
            <% if (settings.qrisStaticImage) { %>
            <div class="mb-3 flex flex-col items-center gap-2">
                <img src="<%= settings.qrisStaticImage %>?t=<%= Date.now() %>" class="rounded-xl border border-emerald-500/30" style="max-width:180px" id="qrisPreviewImg">
                <span class="text-[10px] text-emerald-400 flex items-center gap-1"><iconify-icon icon="mdi:check-circle"></iconify-icon> QRIS aktif</span>
            </div>
            <% } else { %>
            <div class="mb-3 p-4 rounded-xl bg-yellow-500/8 border border-yellow-500/20 text-center">
                <iconify-icon icon="mdi:alert-circle-outline" class="text-yellow-400 text-2xl mb-1 block"></iconify-icon>
                <p class="text-[11px] text-yellow-400">Belum ada gambar QRIS. Upload sekarang.</p>
            </div>
            <% } %>
            <form id="qrisStaticForm" enctype="multipart/form-data" class="space-y-2">
                <input type="file" name="qrisImage" accept="image/jpeg,image/png,image/webp" class="hm-input text-xs py-2">
                <button type="submit" class="btn-primary w-full justify-center py-2.5 text-sm">
                    <iconify-icon icon="mdi:upload"></iconify-icon> Upload Gambar QRIS
                </button>
            </form>
        </div>

        <!-- Pakasir API -->
        <div id="sectionPakasir" class="<%= (settings.qrisMode||'static')==='pakasir' ? '' : 'hidden' %> space-y-3">
            <div class="p-3 rounded-xl bg-red-500/8 border border-red-500/20 text-[11px] text-red-400 mb-2">
                <iconify-icon icon="mdi:alert"></iconify-icon> Pastikan API key dan Project yang dimasukkan sudah benar. Test koneksi sebelum menyimpan.
            </div>
            <form id="pakasirForm" class="space-y-3">
                <div>
                    <div class="setting-row-label">API Key</div>
                    <input type="text" name="apiKey" value="<%= settings.pakasir?.apiKey||'' %>" class="hm-input font-mono text-xs" placeholder="masukkan API key PakKasir">
                </div>
                <div>
                    <div class="setting-row-label">Project Name</div>
                    <input type="text" name="project" value="<%= settings.pakasir?.project||'' %>" class="hm-input" placeholder="nama project kamu">
                </div>
                <div>
                    <div class="setting-row-label">API Base URL <span class="text-zinc-600 font-normal">(hostname, tanpa https://)</span></div>
                    <input type="text" name="apiBaseUrl" value="<%= settings.pakasir?.apiBaseUrl||'app.pakasir.com' %>" class="hm-input font-mono text-xs" placeholder="app.pakasir.com">
                </div>
                <button type="button" onclick="testPakasir()" class="w-full py-2 rounded-xl text-xs font-bold text-zinc-400 border border-white/10 bg-white/3 hover:bg-white/8 transition-all flex items-center justify-center gap-1.5">
                    <iconify-icon icon="mdi:wifi-check"></iconify-icon> Test Koneksi API
                </button>
                <div id="testResultPakasir" class="hidden text-[11px] p-2.5 rounded-lg"></div>
                <button type="submit" class="btn-primary w-full justify-center py-2.5 text-sm">
                    <iconify-icon icon="mdi:content-save"></iconify-icon> Simpan Pengaturan API
                </button>
            </form>
        </div>

        <!-- GensPay API -->
        <div id="sectionGenspay" class="<%= (settings.qrisMode||'static')==='genspay' ? '' : 'hidden' %> space-y-3">
            <div class="p-3 rounded-xl bg-purple-500/8 border border-purple-500/20 text-[11px] text-purple-300 mb-2 space-y-1">
                <div class="font-bold flex items-center gap-1.5">
                    <iconify-icon icon="mdi:book-open-variant"></iconify-icon> Dokumentasi Integrasi
                </div>
                <div>Buka Swagger API: <a href="https://genspay.my.id/docs" target="_blank" class="underline text-purple-400">https://genspay.my.id/docs</a></div>
                <div>Base URL API: <span class="font-mono">https://genspay.my.id/api/v1</span></div>
                <div class="pt-1">Cara pakai:</div>
                <ol class="list-decimal list-inside space-y-0.5">
                    <li>Buat project di Settings → dapat API Key</li>
                    <li>Kirim API Key di header <span class="font-mono">X-API-Key</span></li>
                    <li>Gunakan endpoint create/status/cancel</li>
                </ol>
                <div class="pt-1 text-zinc-400">Ada kendala? Hubungi admin.</div>
            </div>
            <div class="p-3 rounded-xl bg-red-500/8 border border-red-500/20 text-[11px] text-red-400 mb-2">
                <iconify-icon icon="mdi:alert"></iconify-icon> Pastikan API key dan Base URL yang dimasukkan sudah benar. Test koneksi sebelum menyimpan.
            </div>
            <form id="genspayForm" class="space-y-3">
                <div>
                    <div class="setting-row-label">API Key</div>
                    <input type="text" name="apiKey" value="<%= settings.genspay?.apiKey||'' %>" class="hm-input font-mono text-xs" placeholder="masukkan API key GensPay">
                </div>
                <div>
                    <div class="setting-row-label">Base URL API</div>
                    <input type="text" name="baseUrl" value="<%= settings.genspay?.baseUrl||'https://genspay.my.id/api/v1' %>" class="hm-input font-mono text-xs" placeholder="https://genspay.my.id/api/v1">
                </div>
                <div class="p-3 rounded-xl bg-amber-500/8 border border-amber-500/20 text-[11px] text-amber-300 space-y-2">
                    <div class="font-bold flex items-center gap-1.5">
                        <iconify-icon icon="mdi:link-variant"></iconify-icon> Webhook URL — WAJIB diisi di dashboard GensPay
                    </div>
                    <div class="text-zinc-400">Tanpa ini, transaksi bisa nyangkut pending walau sudah dibayar (auto-send jadi lambat/gagal). Copy URL ini ke kolom Webhook URL project GensPay kamu (Settings → Edit project).</div>
                    <div class="flex items-center gap-2">
                        <input type="text" id="genspayWebhookUrl" readonly class="hm-input font-mono text-[11px] flex-1" value="">
                        <button type="button" onclick="copyGenspayWebhook()" class="px-3 py-2 rounded-lg text-xs font-bold text-purple-300 border border-purple-500/30 bg-purple-500/10 hover:bg-purple-500/20 transition-all whitespace-nowrap">
                            <iconify-icon icon="mdi:content-copy"></iconify-icon> Salin
                        </button>
                    </div>
                </div>
                <button type="button" onclick="testGenspay()" class="w-full py-2 rounded-xl text-xs font-bold text-zinc-400 border border-white/10 bg-white/3 hover:bg-white/8 transition-all flex items-center justify-center gap-1.5">
                    <iconify-icon icon="mdi:wifi-check"></iconify-icon> Test Koneksi API
                </button>
                <div id="testResultGenspay" class="hidden text-[11px] p-2.5 rounded-lg"></div>
                <button type="submit" class="btn-primary w-full justify-center py-2.5 text-sm">
                    <iconify-icon icon="mdi:content-save"></iconify-icon> Simpan Pengaturan API
                </button>
            </form>
        </div>

        <!-- Simpan Mode -->
        <input type="hidden" id="qrisModeVal" value="<%= settings.qrisMode||'static' %>">
    </div>

    <!-- Card "Reseller API (Auto Stock Produk / vipibmstore)" DICABUT
         TOTAL (bukan cuma disembunyikan lagi) -- vipibmstore.com tidak
         pernah punya akun beneran, dan integrasinya sempat jadi sumber
         insiden nyata (produk kepasang 'auto' tanpa kredensial pernah
         diisi, customer bayar tapi key gagal terkirim, Sep 2026).
         reseller-api.js sudah DIHAPUS dari repo, endpoint
         /admin/settings/reseller-api, /admin/reseller-api/test,
         /admin/reseller-api/products juga sudah dihapus dari server.js.
         Auto-restock sekarang CUMA lewat GhostSeller Partner API di
         bawah. Kalau suatu saat butuh provider auto lain lagi, harus
         dibangun ulang dari nol, bukan diaktifkan balik dari sini. -->

    <!-- GhostSeller Partner API — Auto Restock -->
    <div class="neon-card p-5 mb-3" style="border-color:rgba(124,47,214,.2)">
        <div class="text-xs font-bold text-violet-400 mb-4 flex items-center gap-2">
            <iconify-icon icon="mdi:autorenew"></iconify-icon> GhostSeller Partner API (Auto Restock)
        </div>
        <p class="text-[11px] text-zinc-500 mb-3">
            Ini integrasi ke platform <strong class="text-violet-300">GhostSeller</strong>
            (ghostseller.my.id) yang juga dikelola sendiri. Varian produk dengan sumber stok
            <strong class="text-violet-300">GhostSeller</strong> akan auto-generate key dari sana tiap ada order, ditagih ke 1 akun
            reseller partner yang sudah disiapkan admin GhostSeller. Atur API Key di sini, lalu pilih sumber stok "GhostSeller" &
            mapping Product/Duration ID di halaman <strong>Edit Produk</strong> masing-masing varian.
        </p>
        <div class="p-3 rounded-xl bg-red-500/8 border border-red-500/20 text-[11px] text-red-400 mb-3">
            <iconify-icon icon="mdi:alert"></iconify-icon> API Key ini bisa dipakai memotong saldo akun partner di GhostSeller.
            Jangan pernah bagikan lewat chat/screenshot. Kalau bocor, regenerate dari
            <span class="font-mono">/dashboard/admin/settings/partner-api</span> di proyek GhostSeller.
        </div>
        <form id="ghostSellerApiForm" class="space-y-3">
            <div>
                <div class="setting-row-label">Base URL Partner API</div>
                <input type="text" name="baseUrl" value="<%= settings.ghostSellerApi?.baseUrl||'https://www.ghostseller.my.id/api/v1/partner' %>" class="hm-input font-mono text-xs" placeholder="https://www.ghostseller.my.id/api/v1/partner">
            </div>
            <div>
                <div class="setting-row-label">API Key (gs_partner_...)</div>
                <input type="text" name="apiKey" value="<%= settings.ghostSellerApi?.apiKey||'' %>" class="hm-input font-mono text-xs" placeholder="gs_partner_...">
            </div>
            <button type="button" onclick="testGhostSellerApi()" class="w-full py-2 rounded-xl text-xs font-bold text-zinc-400 border border-white/10 bg-white/3 hover:bg-white/8 transition-all flex items-center justify-center gap-1.5">
                <iconify-icon icon="mdi:wifi-check"></iconify-icon> Test Koneksi API
            </button>
            <div id="testResultGhostSellerApi" class="hidden text-[11px] p-2.5 rounded-lg"></div>
            <button type="submit" class="btn-primary w-full justify-center py-2.5 text-sm">
                <iconify-icon icon="mdi:content-save"></iconify-icon> Simpan Kredensial GhostSeller
            </button>
        </form>
    </div>

    <!-- Reseller VIP -->
    <div class="neon-card p-5 mb-3" style="border-color:rgba(234,179,8,.2)">
        <div class="text-xs font-bold mb-4 flex items-center gap-2" style="color:#fbbf24">
            <iconify-icon icon="mdi:crown"></iconify-icon> Program Reseller VIP
        </div>
        <form id="resellerSettingsForm" class="space-y-3">
            <div class="flex items-center justify-between p-3 rounded-xl" style="background:rgba(234,179,8,.06);border:1px solid rgba(234,179,8,.15)">
                <div>
                    <div class="text-xs font-semibold text-white">Aktifkan Program Reseller</div>
                    <div class="text-[10px] text-zinc-500 mt-0.5">Tampilkan banner upgrade reseller di halaman utama</div>
                </div>
                <label class="relative inline-flex items-center cursor-pointer">
                    <input type="checkbox" name="resellerEnabled" id="resellerEnabledToggle" class="sr-only peer" <%= (settings.resellerEnabled !== false) ? 'checked' : '' %>>
                    <div class="w-9 h-5 bg-zinc-700 peer-focus:ring-2 peer-focus:ring-yellow-500/30 rounded-full peer peer-checked:bg-yellow-500 transition-all after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:rounded-full after:h-4 after:w-4 after:transition-all peer-checked:after:translate-x-4"></div>
                </label>
            </div>
            <div class="grid grid-cols-2 gap-3">
                <div>
                    <div class="setting-row-label">Biaya Upgrade (Rp)</div>
                    <input type="number" name="resellerPrice" value="<%= settings.resellerPrice || 50000 %>" min="1000" class="hm-input" placeholder="50000">
                </div>
                <div>
                    <div class="setting-row-label">Diskon Reseller (%)</div>
                    <input type="number" name="resellerDiscount" value="<%= settings.resellerDiscount || 20 %>" min="1" max="99" class="hm-input" placeholder="20">
                </div>
            </div>
            <div>
                <div class="setting-row-label">Minimal Top Up Saldo (Rp)</div>
                <input type="number" name="resellerMinDeposit" value="<%= settings.resellerMinDeposit || 100000 %>" min="1000" class="hm-input" placeholder="100000">
                <div class="text-[10px] text-zinc-500 mt-1">Nominal minimum saat reseller top up saldo wallet untuk beli key tanpa scan QRIS berulang.</div>
            </div>
            <div>
                <div class="setting-row-label">Catatan / Tagline</div>
                <input type="text" name="resellerNote" value="<%= settings.resellerNote || 'Dapatkan diskon eksklusif untuk semua produk!' %>" class="hm-input">
            </div>
            <div class="p-3 rounded-xl text-[11px]" style="background:rgba(234,179,8,.06);border:1px solid rgba(234,179,8,.12);color:#a1a1aa">
                <iconify-icon icon="mdi:information-outline" class="text-yellow-400 align-middle mr-1"></iconify-icon>
                Preview: User hemat <strong class="text-yellow-400" id="discPreview"><%= settings.resellerDiscount || 20 %>%</strong> otomatis di semua produk saat login sebagai reseller.
            </div>
            <button type="submit" class="w-full py-2.5 rounded-xl text-xs font-black flex items-center justify-center gap-2" style="background:linear-gradient(135deg,#d97706,#fbbf24);color:#000">
                <iconify-icon icon="mdi:crown"></iconify-icon> Simpan Pengaturan Reseller
            </button>
        </form>
    </div>

    <!-- Kredensial Admin -->
    <div class="neon-card p-5 mb-3">
        <div class="text-xs font-bold text-red-400 mb-4 flex items-center gap-2">
            <iconify-icon icon="mdi:shield-account"></iconify-icon> Kredensial Admin
        </div>
        <form id="credForm" class="space-y-3">
            <div>
                <div class="setting-row-label">Username Admin Baru</div>
                <input type="text" name="adminUsername" value="<%= settings.adminUsername %>" class="hm-input">
            </div>
            <div>
                <div class="setting-row-label">Password Baru</div>
                <input type="password" name="newPassword" class="hm-input" placeholder="Kosongkan jika tidak ingin ubah">
            </div>
            <button type="submit" class="btn-danger w-full justify-center py-2.5 text-sm">
                <iconify-icon icon="mdi:shield-key"></iconify-icon> Update Kredensial
            </button>
        </form>
    </div>

    <!-- Export / Import Data -->
    <div class="neon-card p-5 mb-3">
        <div class="text-xs font-bold text-purple-400 mb-4 flex items-center gap-2">
            <iconify-icon icon="mdi:database-sync"></iconify-icon> Backup & Restore Database
        </div>
        <p class="text-[11px] text-zinc-500 mb-4">Download semua data (produk, transaksi, user, dll) sebagai file JSON. Berguna untuk backup atau pindah server.</p>
        <div class="grid grid-cols-2 gap-3">
            <a href="/admin/export" target="_blank"
               class="flex items-center justify-center gap-2 px-4 py-3 rounded-xl text-xs font-bold transition-all hover:scale-[1.02]"
               style="background:linear-gradient(135deg,rgba(124,47,214,.15),rgba(124,47,214,.08));border:1px solid rgba(124,47,214,.3);color:#38bdf8;">
                <iconify-icon icon="mdi:download"></iconify-icon> Export Database
            </a>
            <div>
                <button onclick="document.getElementById('importInput').click()"
                    class="w-full flex items-center justify-center gap-2 px-4 py-3 rounded-xl text-xs font-bold transition-all hover:scale-[1.02]"
                    style="background:linear-gradient(135deg,rgba(234,179,8,.15),rgba(245,158,11,.08));border:1px solid rgba(234,179,8,.3);color:#fbbf24;">
                    <iconify-icon icon="mdi:upload"></iconify-icon> Import Database
                </button>
                <input type="file" id="importInput" accept=".json" class="hidden" onchange="importDB(event)">
            </div>
        </div>
        <div id="importStatus" class="mt-3 hidden"></div>
    </div>
</div>

</div><!-- /max-w -->

<!-- BOTTOM NAV -->
<nav id="admin-nav" style="overflow-x:auto;white-space:nowrap;display:flex;align-items:center;justify-content:space-around;padding:0 4px 6px;gap:2px;">
    <div class="nav-btn active" id="nav-dashboard" onclick="switchTab('dashboard')">
        <iconify-icon icon="mdi:view-dashboard" class="n-icon"></iconify-icon>
        <span class="n-label">Dashboard</span>
    </div>
    <div class="nav-btn" id="nav-products" onclick="switchTab('products')">
        <iconify-icon icon="mdi:package-variant" class="n-icon"></iconify-icon>
        <span class="n-label">Produk</span>
    </div>
    <div class="nav-btn" id="nav-transactions" onclick="switchTab('transactions')" style="position:relative">
        <iconify-icon icon="mdi:receipt-text" class="n-icon"></iconify-icon>
        <span class="n-label">Transaksi</span>
        <% if(stats.pendingTransactions>0){ %><div class="nav-dot"></div><% } %>
    </div>
    <div class="nav-btn" id="nav-users" onclick="switchTab('users')">
        <iconify-icon icon="mdi:account-group" class="n-icon"></iconify-icon>
        <span class="n-label">Users</span>
    </div>
    <div class="nav-btn" id="nav-keys" onclick="switchTab('keys')">
        <iconify-icon icon="mdi:key-chain" class="n-icon"></iconify-icon>
        <span class="n-label">Keys</span>
    </div>
    <div class="nav-btn" id="nav-vouchers" onclick="switchTab('vouchers')">
        <iconify-icon icon="mdi:ticket-percent" class="n-icon"></iconify-icon>
        <span class="n-label">Voucher</span>
    </div>
    <div class="nav-btn" id="nav-arcade" onclick="switchTab('arcade')">
        <iconify-icon icon="mdi:dice-multiple" class="n-icon"></iconify-icon>
        <span class="n-label">Arcade</span>
    </div>
    <div class="nav-btn" id="nav-settings" onclick="switchTab('settings')">
        <iconify-icon icon="mdi:cog" class="n-icon"></iconify-icon>
        <span class="n-label">Setting</span>
    </div>
</nav>

<!-- MODAL: Tambah / Edit Produk -->
<div id="modal-product" class="modal-wrap">
    <div class="modal-bg" onclick="closeModal('modal-product')"></div>
    <div class="modal-box">
        <div class="modal-handle"></div>
        <div class="p-5">
            <div class="flex items-center justify-between mb-4">
                <h3 class="text-sm font-bold text-white flex items-center gap-2" id="modalProductTitle">
                    <iconify-icon icon="mdi:package-variant-plus" class="text-purple-400"></iconify-icon> Tambah Produk
                </h3>
                <button onclick="closeModal('modal-product')" class="w-7 h-7 rounded-lg bg-white/5 border border-white/10 flex items-center justify-center text-zinc-400">
                    <iconify-icon icon="mdi:close"></iconify-icon>
                </button>
            </div>
            <form id="addProductForm" class="space-y-3" enctype="multipart/form-data">
                <input type="hidden" name="_productId" id="editProductId">
                <div class="grid grid-cols-2 gap-3">
                    <div>
                        <div class="setting-row-label">Nama Produk *</div>
                        <input type="text" name="name" id="prodName" required class="hm-input" placeholder="Drip Client">
                    </div>
                    <div>
                        <div class="setting-row-label">Kategori</div>
                        <select name="category" id="prodCategory" class="hm-input">
                            <% (settings.categories||[]).forEach(cat => { %>
                            <option value="<%= cat %>"><%= (settings.categoryLabels||{})[cat]||cat %></option>
                            <% }) %>
                        </select>
                    </div>
                </div>
                <div>
                    <div class="setting-row-label">Deskripsi</div>
                    <textarea name="description" id="prodDesc" rows="2" class="hm-input" placeholder="Fitur lengkap..."></textarea>
                </div>
                <div>
                    <div class="setting-row-label">Gambar (URL atau upload)</div>
                    <input type="text" name="imageUrl" id="prodImageUrl" class="hm-input mb-2" placeholder="https://cdn.../gambar.png">
                    <input type="file" name="image" accept="image/*" class="hm-input text-xs py-2">
                </div>

                <!-- PRICING TABLE (like deem) -->
                <div>
                    <div class="flex items-center justify-between mb-2">
                        <div class="setting-row-label mb-0">Opsi Harga *</div>
                        <button type="button" onclick="addPricingRow()" class="flex items-center gap-1 text-[11px] text-purple-400 hover:text-purple-300 transition-colors">
                            <iconify-icon icon="mdi:plus-circle"></iconify-icon> Tambah Durasi
                        </button>
                    </div>
                    <div id="pricingRows" class="space-y-2">
                        <div class="flex items-center gap-2 text-[9px] font-bold uppercase tracking-wider text-zinc-600 px-0.5">
                            <div style="flex:0 0 56px">Durasi</div>
                            <div style="flex:0 0 74px">Unit</div>
                            <div class="flex-1">Harga Normal</div>
                            <div class="flex-1">Harga Reseller <span class="normal-case font-normal text-zinc-700">(kosong = pakai %)</span></div>
                            <div style="width:32px"></div>
                        </div>
                        <!-- rows injected by JS -->
                    </div>
                    <p class="text-[10px] text-zinc-600 mt-1">Key format: <code class="text-purple-400">KEY123</code> (generic) atau <code class="text-purple-400">KEY123:7</code> (khusus paket dengan angka durasi 7, berapapun unitnya)</p>
                </div>

                <!-- STATUS -->
                <div>
                    <div class="setting-row-label">Status</div>
                    <select name="status" id="prodStatus" class="hm-input">
                        <option value="active">Aktif</option>
                        <option value="inactive">Nonaktif</option>
                    </select>
                </div>

                <!-- Toggle Mode Stok "Manual vs Auto (Reseller API)" DIHAPUS TOTAL.
                     vipibmstore sudah dicabut dari sistem ini -- lihat catatan
                     panjang di resolveProductKey()/server.js. Produk baru
                     SELALU dibuat Manual (upload key sendiri di bawah); kalau
                     mau pakai auto-restock GhostSeller untuk varian tertentu,
                     atur itu belakangan lewat halaman Edit Produk (Stock
                     Source per-baris durasi), bukan di modal Tambah ini. -->

                <!-- KEYS TEXTAREA -->
                <div id="prodKeysSection">
                    <div class="flex items-center justify-between mb-1">
                        <div class="setting-row-label mb-0">Keys / Lisensi</div>
                        <span id="prodKeyCount" class="text-[10px] text-zinc-500"></span>
                    </div>
                    <textarea name="keys" id="prodKeys" rows="5" class="hm-input font-mono text-xs"
                        placeholder="KEY-FF-001&#10;KEY-FF-002:7&#10;KEY-FF-003:30&#10;&#10;Kosongkan = tidak ubah keys (mode edit)"></textarea>
                    <div class="flex gap-2 mt-1" id="keysModeRow">
                        <label class="flex items-center gap-1 text-[11px] text-zinc-400 cursor-pointer">
                            <input type="radio" name="keysMode" value="append" checked class="accent-indigo-500"> Tambah ke stok existing
                        </label>
                        <label class="flex items-center gap-1 text-[11px] text-zinc-400 cursor-pointer">
                            <input type="radio" name="keysMode" value="replace" class="accent-red-500"> Ganti semua keys
                        </label>
                    </div>
                </div>

                <div class="flex gap-2 pt-1">
                    <button type="submit" id="prodSubmitBtn" class="btn-primary flex-1 justify-center py-2.5 text-xs">
                        <iconify-icon icon="mdi:plus"></iconify-icon> <span id="prodSubmitLabel">Tambah Produk</span>
                    </button>
                    <button type="button" onclick="closeModal('modal-product')" class="btn-danger justify-center py-2.5 px-4 text-xs">Batal</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- MODAL: Kelola Keys Cepat -->
<div id="modal-keys" class="modal-wrap">
    <div class="modal-bg" onclick="closeModal('modal-keys')"></div>
    <div class="modal-box">
        <div class="modal-handle"></div>
        <div class="p-5">
            <div class="flex items-center justify-between mb-2">
                <h3 class="text-sm font-bold text-white flex items-center gap-2">
                    <iconify-icon icon="mdi:key-plus" class="text-emerald-400"></iconify-icon>
                    Keys: <span id="keysModalProductName" class="text-purple-300"></span>
                </h3>
                <button onclick="closeModal('modal-keys')" class="w-7 h-7 rounded-lg bg-white/5 border border-white/10 flex items-center justify-center text-zinc-400">
                    <iconify-icon icon="mdi:close"></iconify-icon>
                </button>
            </div>
            <div class="mb-3 flex items-center gap-2 p-2.5 rounded-lg bg-indigo-500/8 border border-indigo-500/15">
                <iconify-icon icon="mdi:database" class="text-purple-400 text-sm"></iconify-icon>
                <span class="text-xs text-zinc-400">Stok saat ini: <strong class="text-white" id="keysCurrentCount">—</strong> keys</span>
            </div>
            <form id="addKeysForm" class="space-y-3">
                <input type="hidden" id="keysProductId">
                <div>
                    <div class="setting-row-label">Durasi key ini</div>
                    <select id="keysDuration" class="hm-input text-xs">
                        <option value="">Generic (semua durasi)</option>
                    </select>
                    <p class="text-[10px] text-zinc-600 mt-1">Pilih durasi supaya key yang ditempel di bawah otomatis ditandai untuk durasi itu — tidak perlu ketik format KEY:HARI manual lagi.</p>
                </div>
                <div>
                    <div class="setting-row-label">Keys Baru (satu baris = satu key)</div>
                    <textarea id="keysTextarea" rows="8" class="hm-input font-mono text-xs"
                        placeholder="KEY-FF-001&#10;KEY-FF-002&#10;KEY-FF-003&#10;&#10;Atau tempel KEY:HARI manual kalau mau override per baris"></textarea>
                </div>
                <div class="flex gap-3">
                    <label class="flex items-center gap-1.5 text-[11px] text-zinc-400 cursor-pointer">
                        <input type="radio" name="keysAddMode" value="append" checked class="accent-indigo-500"> Tambahkan
                    </label>
                    <label class="flex items-center gap-1.5 text-[11px] text-zinc-400 cursor-pointer">
                        <input type="radio" name="keysAddMode" value="replace" class="accent-red-500"> Ganti semua
                    </label>
                </div>
                <div class="flex gap-2">
                    <button type="submit" id="addKeysSubmitBtn" class="btn-success flex-1 justify-center py-2.5 text-xs">
                        <iconify-icon icon="mdi:content-save"></iconify-icon> Simpan Keys
                    </button>
                    <button type="button" onclick="closeModal('modal-keys')" class="btn-danger py-2.5 px-4 text-xs">Batal</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- MODAL: Harga Reseller Per Produk -->
<div id="modal-reseller-price" class="modal-wrap">
    <div class="modal-bg" onclick="closeModal('modal-reseller-price')"></div>
    <div class="modal-box">
        <div class="modal-handle"></div>
        <div class="p-5">
            <div class="flex items-center justify-between mb-3">
                <h3 class="text-sm font-bold text-white flex items-center gap-2">
                    <iconify-icon icon="mdi:crown" class="text-yellow-400"></iconify-icon>
                    Harga Reseller: <span id="rpModalProductName" class="text-purple-300"></span>
                </h3>
                <button onclick="closeModal('modal-reseller-price')" class="w-7 h-7 rounded-lg bg-white/5 border border-white/10 flex items-center justify-center text-zinc-400">
                    <iconify-icon icon="mdi:close"></iconify-icon>
                </button>
            </div>
            <div class="mb-3 flex items-start gap-2 p-2.5 rounded-lg bg-indigo-500/8 border border-indigo-500/15">
                <iconify-icon icon="mdi:information-outline" class="text-purple-400 text-sm mt-0.5"></iconify-icon>
                <span class="text-xs text-zinc-400">Set harga reseller per durasi. Kosongkan salah satu untuk otomatis ikut diskon % global dari menu Settings.</span>
            </div>
            <input type="hidden" id="rpProductId">
            <div id="rpRowsWrap" class="space-y-2 mb-3"></div>
            <div id="rpEmptyMsg" class="text-center py-6 text-zinc-600 text-xs" style="display:none">
                Produk ini belum punya opsi harga. Tambahkan durasi di menu Edit dulu.
            </div>
            <button type="button" onclick="autoFillResellerPrices()" id="rpAutoBtn" class="w-full mb-3 py-2 text-xs font-bold rounded-lg flex items-center justify-center gap-1.5"
                style="background:rgba(124,47,214,.12);border:1px solid rgba(124,47,214,.3);color:#7dd3fc">
                <iconify-icon icon="mdi:auto-fix"></iconify-icon> Auto Harga Reseller
            </button>
            <div class="flex gap-2">
                <button type="button" onclick="saveResellerPrices()" id="rpSaveBtn" class="btn-success flex-1 justify-center py-2.5 text-xs">
                    <iconify-icon icon="mdi:content-save"></iconify-icon> Simpan Harga Reseller
                </button>
                <button type="button" onclick="closeModal('modal-reseller-price')" class="btn-danger py-2.5 px-4 text-xs">Batal</button>
            </div>
        </div>
    </div>
</div>

<!-- MODAL: Tambah Testimoni -->
<div id="modal-testi" class="modal-wrap">
    <div class="modal-bg" onclick="closeModal('modal-testi')"></div>
    <div class="modal-box">
        <div class="modal-handle"></div>
        <div class="p-5">
            <div class="flex items-center justify-between mb-4">
                <h3 class="text-sm font-bold text-white flex items-center gap-2">
                    <iconify-icon icon="mdi:comment-plus" class="text-purple-400"></iconify-icon> Tambah Testimoni
                </h3>
                <button onclick="closeModal('modal-testi')" class="w-7 h-7 rounded-lg bg-white/5 border border-white/10 flex items-center justify-center text-zinc-400">
                    <iconify-icon icon="mdi:close"></iconify-icon>
                </button>

            </div>
            <form id="addTestimonialForm" class="space-y-3">
                <div class="grid grid-cols-2 gap-3">
                    <div>
                        <div class="setting-row-label">Nama *</div>
                        <input type="text" name="name" required class="hm-input" placeholder="Nama pembeli">
                    </div>
                    <div>
                        <div class="setting-row-label">Rating *</div>
                        <select name="rating" required class="hm-input">
                            <option value="5">★★★★★ (5)</option>
                            <option value="4">★★★★☆ (4)</option>
                            <option value="3">★★★☆☆ (3)</option>
                            <option value="2">★★☆☆☆ (2)</option>
                            <option value="1">★☆☆☆☆ (1)</option>
                        </select>
                    </div>
                </div>
                <div>
                    <div class="setting-row-label">Produk (opsional)</div>
                    <input type="text" name="product" class="hm-input" placeholder="Nama produk">
                </div>
                <div>
                    <div class="setting-row-label">Isi Testimoni *</div>
                    <textarea name="text" rows="3" required class="hm-input" placeholder="Tulis testimoni..."></textarea>
                </div>
                <div class="flex gap-4">
                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" name="verified" checked class="w-3.5 h-3.5 accent-indigo-500">
                        <span class="text-xs text-zinc-400">Verified</span>
                    </label>
                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" name="featured" checked class="w-3.5 h-3.5 accent-purple-500">
                        <span class="text-xs text-zinc-400">Featured (tampil di home)</span>
                    </label>
                </div>
                <div class="flex gap-2 pt-1">
                    <button type="submit" class="btn-primary flex-1 justify-center py-2.5 text-xs">
                        <iconify-icon icon="mdi:plus"></iconify-icon> Tambah
                    </button>
                    <button type="button" onclick="closeModal('modal-testi')" class="btn-danger py-2.5 px-4 text-xs">Batal</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
window.__gnProducts = <%- JSON.stringify(products.map(p => ({ id: p.id, name: p.name, keyCount: (p.keys||[]).length }))) %>;
window.__gnResellerDiscount = <%= parseInt(settings.resellerDiscount) || 20 %>;
// ── TOAST ──
function toast(msg, type='info') {
    const styles = {
        success: 'border-green-500/40 text-green-300',
        error:   'border-red-500/40 text-red-300',
        info:    'border-indigo-500/40 text-purple-300',
        warn:    'border-yellow-500/40 text-yellow-300'
    };
    const icons = { success:'mdi:check-circle', error:'mdi:alert-circle', info:'mdi:information', warn:'mdi:alert' };
    const el = document.createElement('div');
    el.className = `toast ${styles[type]||styles.info}`;
    el.innerHTML = `<iconify-icon icon="${icons[type]||icons.info}" class="text-base flex-shrink-0"></iconify-icon><span>${msg}</span>`;
    document.getElementById('toast-container').appendChild(el);
    setTimeout(() => { el.style.opacity='0'; el.style.transition='opacity .3s'; setTimeout(()=>el.remove(),300); }, 3000);
}

// ── LOGOUT ──
function clearAndLogout(e) {
    e.preventDefault();
    localStorage.removeItem('hm_cart');
    localStorage.removeItem('shownNotifs');
    window.location.href = '/logout';
}

// ── SINGLE-DEVICE ADMIN LOCK ──
// Server membalas { sessionRevoked: true } kalau sesi admin ini sudah
// digantikan login dari perangkat lain. Kita pasang sekali di window.fetch
// supaya SEMUA pemanggilan fetch() di halaman ini otomatis kebagian deteksi,
// tanpa perlu ubah satu-satu di tiap tombol/aksi.
let _sessionKicked = false;
const _origFetch = window.fetch.bind(window);
window.fetch = async function(...args) {
    const res = await _origFetch(...args);
    if (!_sessionKicked && res.status === 401) {
        try {
            const clone = res.clone();
            const ct = clone.headers.get('content-type') || '';
            if (ct.includes('application/json')) {
                const data = await clone.json();
                if (data && data.sessionRevoked) {
                    _sessionKicked = true;
                    toast(data.message || 'Sesi admin diakhiri karena login dari perangkat lain', 'error');
                    setTimeout(() => { window.location.href = '/vpr-secure-panel-8x?kicked=1'; }, 1800);
                }
            }
        } catch {}
    }
    return res;
};

// Heartbeat: kabari server tab ini masih aktif, supaya lock tidak expired
// selama admin masih membuka panel (tapi tetap auto-expired kalau tab
// ditutup/koneksi putus, lihat ADMIN_LOCK_TIMEOUT_MS di server).
setInterval(() => {
    if (_sessionKicked || document.hidden) return;
    fetch('/admin/session/heartbeat', { method: 'POST' }).catch(() => {});
}, 90000);


// ── CEK STATUS DATABASE (SUPABASE) ──
(async function checkDbStatus() {
    try {
        const r = await fetch('/admin/db-status');
        const d = await r.json();
        const alertEl = document.getElementById('binid-alert');
        const msgEl   = document.getElementById('binid-msg');
        const content = document.getElementById('dbStatusContent');

        if (!d.hasUrl || !d.hasKey) {
            if (d.hasAnonKeyOnly) {
                if (alertEl) { alertEl.classList.remove('hidden'); msgEl.innerHTML = `⚠️ Yang ke-set itu SUPABASE_ANON_KEY — tapi app ini butuh SUPABASE_SERVICE_ROLE_KEY (beda key!). Lihat Settings → API di dashboard Supabase, copy yang "service_role" (secret), bukan "anon" (public).`; }
                if (content) content.innerHTML = `<div class="flex items-start gap-2 text-red-400"><iconify-icon icon="mdi:alert-circle" class="text-base flex-shrink-0 mt-0.5"></iconify-icon><div><strong>Salah key: app butuh service_role, bukan anon.</strong><br>Env var <code class="bg-black/30 px-1 rounded">SUPABASE_ANON_KEY</code> tidak dibaca sama sekali oleh server ini. Buat env var baru bernama persis <code class="bg-black/30 px-1 rounded">SUPABASE_SERVICE_ROLE_KEY</code> isi dengan secret "service_role" dari Supabase (Project Settings → API), lalu Redeploy.</div></div>`;
            } else {
                const missing = [!d.hasUrl && 'SUPABASE_URL', !d.hasKey && 'SUPABASE_SERVICE_ROLE_KEY'].filter(Boolean).join(' & ');
                if (alertEl) { alertEl.classList.remove('hidden'); msgEl.innerHTML = `⚠️ Env var ${missing} tidak terbaca server ini. Set lalu Redeploy di Vercel (perubahan env var TIDAK otomatis kepakai tanpa redeploy).`; }
                if (content) content.innerHTML = `<div class="flex items-start gap-2 text-red-400"><iconify-icon icon="mdi:alert-circle" class="text-base flex-shrink-0 mt-0.5"></iconify-icon><div><strong>${missing} tidak terbaca di server ini.</strong><br>Env var sudah benar di dashboard Vercel? Coba <b>Redeploy</b> — Vercel tidak otomatis menerapkan env var baru/yang diubah ke deployment yang sudah berjalan.</div></div>`;
            }
        } else if (!d.connected && d.projectPaused) {
            const restoreLink = d.restoreUrl ? `<a href="${d.restoreUrl}" target="_blank" class="underline font-bold">Restore Project di Supabase</a>` : 'dashboard Supabase';
            if (alertEl) { alertEl.classList.remove('hidden'); msgEl.innerHTML = `⚠️ Project Supabase kemungkinan PAUSED atau tabel belum dibuat. Buka dashboard Supabase → restore project → lalu jalankan supabase-schema.sql.`; }
            if (content) content.innerHTML = `<div class="flex items-start gap-2 text-amber-400"><iconify-icon icon="mdi:pause-circle" class="text-base flex-shrink-0 mt-0.5"></iconify-icon><div><strong>Project Supabase kemungkinan besar sedang PAUSED.</strong><br><span class="text-zinc-400 text-[11px]">Supabase free tier auto-pause project setelah 7 hari tidak ada aktivitas. Error kosong <code class="bg-black/30 px-1 rounded">{"message":""}</code> adalah tanda khasnya.</span><br>→ ${restoreLink} dulu, tunggu sampai statusnya "Active", baru coba reload halaman ini.<br><span class="text-zinc-500 text-[11px]">Kalau project sudah Active tapi masih error, berarti tabel belum dibuat — jalankan <code class="bg-black/30 px-1 rounded">supabase-schema.sql</code> di ${d.sqlEditorUrl ? `<a href="${d.sqlEditorUrl}" target="_blank" class="underline">SQL Editor</a>` : 'SQL Editor'}.</span></div></div>`;
        } else if (d.connected && !d.tableExists) {
            if (alertEl) { alertEl.classList.remove('hidden'); msgEl.innerHTML = `⚠️ Gagal konek ke Supabase: ${d.errorMsg || 'unknown error'}`; }
            if (content) content.innerHTML = `<div class="text-red-400">Gagal terkoneksi ke Supabase.<br><span class="text-zinc-500 text-[11px]">${d.errorMsg || 'Tidak ada detail error dari server.'}</span></div>`;
        } else if (!d.tableExists) {
            if (alertEl) { alertEl.classList.remove('hidden'); msgEl.innerHTML = `⚠️ Tabel keyvalue_store belum ada di Supabase. Jalankan supabase-schema.sql di SQL Editor.`; }
            if (content) content.innerHTML = `<div class="flex items-start gap-2 text-red-400"><iconify-icon icon="mdi:alert-circle" class="text-base flex-shrink-0 mt-0.5"></iconify-icon><div><strong>Terkoneksi, tapi tabel keyvalue_store belum ada.</strong><br>Jalankan isi file <code class="bg-black/30 px-1 rounded">supabase-schema.sql</code> di ${d.sqlEditorUrl ? `<a href="${d.sqlEditorUrl}" target="_blank" class="underline">SQL Editor Supabase</a>` : 'SQL Editor Supabase'}.</div></div>`;
        } else {
            if (content) content.innerHTML = `<div class="flex items-center gap-2 text-emerald-400"><iconify-icon icon="mdi:check-circle" class="text-base"></iconify-icon><div>Database terhubung ke Supabase PostgreSQL.<br><span class="text-zinc-500">Project: <code class="text-zinc-400">${d.projectUrl || '—'}</code></span></div></div>`;
        }
    } catch(e) {
        const content = document.getElementById('dbStatusContent');
        if (content) content.innerHTML = `<div class="text-red-400">Gagal cek status database: ${e.message}</div>`;
    }
})();


// ── TAB SWITCH ──
function switchTab(name) {
    document.querySelectorAll('.tab-pane').forEach(p => p.classList.remove('active'));
    document.querySelectorAll('.nav-btn').forEach(b => b.classList.remove('active'));
    document.getElementById('pane-'+name).classList.add('active');
    const nb = document.getElementById('nav-'+name);
    if (nb) nb.classList.add('active');
    window.scrollTo({top:0,behavior:'smooth'});
    if (name === 'testimonials') loadTestimonials();
    if (name === 'keys') loadKeys();
    if (name === 'vouchers') loadVouchers();
    history.replaceState(null, '', '/admin#' + name);
}

// ── HASH ROUTING: auto switch tab dari URL hash ──
document.addEventListener('DOMContentLoaded', () => {
    const validTabs = ['dashboard','products','transactions','users','testimonials','settings','keys','vouchers','arcade'];
    const hash = window.location.hash.replace('#', '');
    if (validTabs.includes(hash)) switchTab(hash);
});

// Isi otomatis kolom Webhook URL GensPay pakai origin domain yang lagi
// dipakai admin buka panel ini, biar URL-nya selalu benar tanpa perlu
// diketik manual — sumber laporan "transaksi pending" adalah field ini
// yang kosong dan tidak pernah didaftarkan ke dashboard GensPay.
(function initGenspayWebhookUrl() {
    const el = document.getElementById('genspayWebhookUrl');
    if (el) el.value = window.location.origin + '/webhook/genspay';
})();

function copyGenspayWebhook() {
    const el = document.getElementById('genspayWebhookUrl');
    if (!el) return;
    el.select();
    el.setSelectionRange(0, 99999);
    navigator.clipboard.writeText(el.value).then(() => {
        toast('Webhook URL disalin!', 'success');
    }).catch(() => {
        document.execCommand('copy');
        toast('Webhook URL disalin!', 'success');
    });
}


// ── MODAL ──
function openModal(id) { document.getElementById(id).classList.add('open'); document.body.style.overflow='hidden'; }
function closeModal(id) { document.getElementById(id).classList.remove('open'); document.body.style.overflow=''; }
// (openAddProductModal, openEditProductModal, openAddKeysModal defined above)
document.addEventListener('keydown', e => {
    if (e.key === 'Escape') document.querySelectorAll('.modal-wrap.open').forEach(m => m.classList.remove('open'));
});

// ── API helper ──
async function api(url, data) {
    const r = await fetch(url, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data)
    });
    return r.json();
}

// ── PRODUK: Pricing rows ──
// unit ('hari'/'jam'/'menit') CUMA untuk label tampilan admin — angka yang
// diinput ("Durasi") disimpan apa adanya ke field `days` yang sudah dipakai
// luas di seluruh sistem (key tag "KEY:N", matching item saat checkout,
// resolveStockSourceForDays, dst). Kita SENGAJA tidak mengonversi ke pecahan
// hari, supaya regex & logic matching lama ("N DAYS") tetap jalan tanpa
// disentuh -- kalau admin pilih "Jam", cukup anggap N itu "paket ke-N" dan
// beri key stok terpisah dengan tag :N seperti biasa, cuma labelnya beda.
let pricingRowCount = 0;
function addPricingRow(days='', price='', resellerPrice='', unit='hari') {
    const i = pricingRowCount++;
    const row = document.createElement('div');
    row.className = 'flex items-center gap-2';
    row.innerHTML = `
        <div class="flex-1 flex gap-2">
            <div style="flex:0 0 56px">
                <input type="number" name="pricingDays" value="${days}" min="1" required
                    class="hm-input text-xs" placeholder="Durasi">
            </div>
            <div style="flex:0 0 74px">
                <select name="pricingUnits" class="hm-input text-xs" style="padding-left:6px;padding-right:6px">
                    <option value="hari" ${unit==='hari'?'selected':''}>Hari</option>
                    <option value="jam" ${unit==='jam'?'selected':''}>Jam</option>
                    <option value="menit" ${unit==='menit'?'selected':''}>Menit</option>
                </select>
            </div>
            <div class="flex-1">
                <input type="number" name="pricingPrices" value="${price}" min="0" required
                    class="hm-input text-xs" placeholder="Harga Normal (Rp)">
            </div>
            <div class="flex-1">
                <input type="number" name="pricingResellerPrices" value="${resellerPrice}" min="0"
                    class="hm-input text-xs" placeholder="Harga Reseller (Rp)" title="Kosongkan = ikut diskon % global">
            </div>
        </div>
        <button type="button" onclick="this.closest('.flex').remove()" class="w-8 h-8 rounded-lg bg-red-500/10 border border-red-500/20 flex items-center justify-center text-red-400 flex-shrink-0">
            <iconify-icon icon="mdi:minus" class="text-sm"></iconify-icon>
        </button>`;
    document.getElementById('pricingRows').appendChild(row);
}

function openAddProductModal() {
    document.getElementById('editProductId').value = '';
    document.getElementById('modalProductTitle').innerHTML = '<iconify-icon icon="mdi:package-variant-plus" class="text-purple-400"></iconify-icon> Tambah Produk';
    document.getElementById('prodSubmitLabel').textContent = 'Tambah Produk';
    document.getElementById('addProductForm').reset();
    document.getElementById('pricingRows').innerHTML = '';
    document.getElementById('prodKeyCount').textContent = '';
    document.getElementById('keysModeRow').style.display = 'none';
    pricingRowCount = 0;
    addPricingRow('7','60000'); addPricingRow('30','150000');
    openModal('modal-product');
}

function openEditProductModal(id, name, category, description, imageUrl, pricing, keyCount, status) {
    document.getElementById('editProductId').value = id;
    document.getElementById('modalProductTitle').innerHTML = '<iconify-icon icon="mdi:pencil" class="text-yellow-400"></iconify-icon> Edit Produk';
    document.getElementById('prodSubmitLabel').textContent = 'Simpan Perubahan';
    document.getElementById('prodName').value = name||'';
    document.getElementById('prodDesc').value = description||'';
    document.getElementById('prodImageUrl').value = imageUrl||'';
    document.getElementById('prodKeys').value = '';
    document.getElementById('prodKeyCount').textContent = `${keyCount} key aktif`;
    document.getElementById('keysModeRow').style.display = 'flex';
    // Set category
    const catSel = document.getElementById('prodCategory');
    if (catSel) { for(let o of catSel.options) { o.selected = (o.value===category); } }
    // Set status
    const stSel = document.getElementById('prodStatus');
    if (stSel) { for(let o of stSel.options) { o.selected = (o.value===status); } }
    // Pricing
    document.getElementById('pricingRows').innerHTML = '';
    pricingRowCount = 0;
    if (pricing && pricing.length) { pricing.forEach(o => addPricingRow(o.days, o.price, o.reseller_price || '', o.unit || 'hari')); }
    else addPricingRow('7','60000');
    openModal('modal-product');
}

document.getElementById('addProductForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    const btn = document.getElementById('prodSubmitBtn');
    const editId = document.getElementById('editProductId').value;
    btn.disabled = true;
    btn.innerHTML = '<iconify-icon icon="mdi:loading" class="animate-spin"></iconify-icon> Menyimpan...';
    const fd = new FormData(this);
    const url = editId ? `/admin/product/edit/${editId}` : '/admin/product/add';
    try {
        const r = await fetch(url, { method:'POST', body:fd });
        const d = await r.json();
        if (d.success) {
            toast(editId ? 'Produk diupdate!' : 'Produk ditambahkan!', 'success');
            closeModal('modal-product');
            setTimeout(()=>{ window.location.href = '/admin#products'; },500);
        } else {
            toast(d.message||'Gagal menyimpan produk','error');
            console.error('[add/edit product] server error:', d);
        }
    } catch(err) {
        toast('Error koneksi: '+err.message,'error');
        console.error('[add/edit product] fetch error:', err);
    }
    btn.disabled = false;
    btn.innerHTML = editId ? '<iconify-icon icon="mdi:pencil"></iconify-icon> Simpan Perubahan' : '<iconify-icon icon="mdi:plus"></iconify-icon> Tambah Produk';
});

// ── KEYS MODAL ──
function openAddKeysModal(pid, productName, currentCount, pricingJson) {
    document.getElementById('keysProductId').value = pid;
    document.getElementById('keysModalProductName').textContent = productName||pid;
    document.getElementById('keysCurrentCount').textContent = currentCount||'?';
    document.getElementById('keysTextarea').value = '';

    // Isi dropdown durasi dari pricing options produk ini, supaya admin
    // tinggal pilih durasi alih-alih ketik format KEY:HARI manual per baris.
    const durationSelect = document.getElementById('keysDuration');
    durationSelect.innerHTML = '<option value="">Generic (semua durasi)</option>';
    let pricing = [];
    try { pricing = pricingJson ? JSON.parse(pricingJson) : []; } catch { pricing = []; }
    pricing.forEach(po => {
        if (!po.days) return;
        const opt = document.createElement('option');
        opt.value = po.days;
        // FIX: sebelumnya selalu hardcode "Hari" apapun unit aslinya -- kalau
        // produk punya paket "1 Jam" DAN "1 Hari", dropdown ini nampilin dua
        // opsi "1 Hari" yang keliatan identik padahal beda unit, bikin admin
        // salah pilih durasi pas restock manual.
        const unitLabel = po.unit === 'jam' ? 'Jam' : po.unit === 'menit' ? 'Menit' : 'Hari';
        opt.textContent = `${po.days} ${unitLabel}`;
        durationSelect.appendChild(opt);
    });

    openModal('modal-keys');
}

document.getElementById('addKeysForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    // FIX BUG "key jadi double pas restock": tombol ini sebelumnya tidak
    // pernah didisable saat proses submit, jadi klik ganda (koneksi lemot,
    // tidak ada indikasi loading) bisa mengirim 2 request nyaris bersamaan
    // dan menggandakan key yang di-restock. Backend sudah dedup juga sebagai
    // lapis kedua, tapi ini mencegahnya dari sumbernya.
    const btn = document.getElementById('addKeysSubmitBtn');
    if (btn.disabled) return;
    btn.disabled = true;
    const originalHtml = btn.innerHTML;
    btn.innerHTML = '<iconify-icon icon="mdi:loading" class="animate-spin"></iconify-icon> Menyimpan...';
    const pid      = document.getElementById('keysProductId').value;
    const keys     = document.getElementById('keysTextarea').value.trim();
    const mode     = document.querySelector('input[name=keysAddMode]:checked')?.value || 'append';
    const duration = document.getElementById('keysDuration').value;
    if (!keys) { toast('Masukkan minimal 1 key','error'); btn.disabled = false; btn.innerHTML = originalHtml; return; }
    try {
        const d = await api(`/admin/product/keys/${pid}`, { keys, mode, duration });
        if (d.success) {
            toast(`Keys disimpan! Total: ${d.keyCount} key`, 'success');
            closeModal('modal-keys');
            const card = document.querySelector(`[data-product-id="${pid}"]`);
            if (card) { const span = card.querySelector('.prod-key-count'); if(span) span.textContent = `${d.keyCount} key`; }
        } else toast(d.message, 'error');
    } catch(err) {
        toast('Error koneksi: '+err.message, 'error');
    }
    btn.disabled = false;
    btn.innerHTML = originalHtml;
});

// ── MODAL: HARGA RESELLER PER PRODUK ──
// Dipakai buat set harga_reseller per durasi tanpa buka halaman Edit Produk
// penuh. Field yang dikosongkan = otomatis ikut diskon % global (lihat
// Settings > Reseller). Tombol "Auto Harga Reseller" cuma BANTU isi field
// berdasarkan diskon % global saat ini -- hasilnya tetap bisa diubah admin
// sebelum disimpan, tidak dikunci/di-lock ke hasil auto.
let _rpPricing = []; // pricing options produk yang lagi dibuka di modal ini

function openResellerPriceModal(pid, productName, pricingJson) {
    document.getElementById('rpProductId').value = pid;
    document.getElementById('rpModalProductName').textContent = productName || pid;
    try { _rpPricing = pricingJson ? JSON.parse(pricingJson) : []; } catch { _rpPricing = []; }
    renderRpRows();
    openModal('modal-reseller-price');
}

function renderRpRows() {
    const wrap = document.getElementById('rpRowsWrap');
    const emptyMsg = document.getElementById('rpEmptyMsg');
    const autoBtn = document.getElementById('rpAutoBtn');
    const saveBtn = document.getElementById('rpSaveBtn');
    if (!_rpPricing.length) {
        wrap.innerHTML = '';
        emptyMsg.style.display = '';
        autoBtn.style.display = 'none';
        saveBtn.style.display = 'none';
        return;
    }
    emptyMsg.style.display = 'none';
    autoBtn.style.display = '';
    saveBtn.style.display = '';
    wrap.innerHTML = _rpPricing.map((o, i) => `
        <div class="flex items-center gap-2">
            <div class="w-16 flex-shrink-0 text-xs text-zinc-400 font-bold">${o.days} ${o.unit === 'jam' ? 'Jam' : o.unit === 'menit' ? 'Menit' : 'Hari'}</div>
            <div class="w-20 flex-shrink-0 text-[10px] text-zinc-600">Rp ${(o.price||0).toLocaleString('id-ID')}</div>
            <input type="number" min="0" class="hm-input text-xs flex-1 rp-input" data-idx="${i}"
                value="${o.reseller_price != null ? o.reseller_price : ''}"
                placeholder="Harga Reseller (Rp)" title="Kosongkan = ikut diskon % global">
        </div>
    `).join('');
}

// Isi otomatis SEMUA baris berdasarkan diskon % global saat ini. Admin
// tetap bisa mengubah hasilnya manual sebelum klik Simpan -- ini cuma
// shortcut pengisian, bukan penguncian nilai.
function autoFillResellerPrices() {
    const discount = window.__gnResellerDiscount || 20;
    document.querySelectorAll('#rpRowsWrap .rp-input').forEach(inp => {
        const idx = parseInt(inp.dataset.idx);
        const opt = _rpPricing[idx];
        if (!opt) return;
        inp.value = Math.round((opt.price || 0) * (1 - discount / 100));
    });
    toast(`Harga reseller diisi otomatis (diskon ${discount}%). Masih bisa diubah sebelum disimpan.`, 'success');
}

async function saveResellerPrices() {
    const pid = document.getElementById('rpProductId').value;
    if (!pid || !_rpPricing.length) return;

    // Kirim ULANG SELURUH pricingOptions (bukan cuma reseller_price) supaya
    // field lain per baris (stockSource, resellerItemId, price, days) yang
    // sudah tersimpan tidak ikut ke-reset oleh endpoint update produk --
    // lihat catatan "BUG SEBELUMNYA" di POST /admin/product/:id server.js.
    const inputs = document.querySelectorAll('#rpRowsWrap .rp-input');
    const updated = _rpPricing.map((o, i) => ({ ...o }));
    inputs.forEach(inp => {
        const idx = parseInt(inp.dataset.idx);
        const val = inp.value.trim();
        updated[idx].reseller_price = (val !== '' && !isNaN(parseInt(val)) && parseInt(val) >= 0) ? parseInt(val) : null;
    });

    const summaryStockMode = (() => {
        const autoCount = updated.filter(o => (o.stockSource || 'auto') !== 'manual').length;
        if (autoCount === updated.length) return 'auto';
        if (autoCount === 0) return 'manual';
        return 'mixed';
    })();

    const d = await api(`/admin/product/${pid}`, { pricingOptions: updated, stockMode: summaryStockMode });
    if (d.success) {
        toast('Harga reseller berhasil disimpan!', 'success');
        closeModal('modal-reseller-price');
        const card = document.querySelector(`[data-product-id="${pid}"]`);
        if (card) {
            const btn = card.querySelector('[onclick^="openResellerPriceModal"]');
            if (btn) btn.dataset.pricing = JSON.stringify(updated).replace(/"/g, '&quot;');
        }
    } else toast(d.message || 'Gagal menyimpan harga reseller', 'error');
}

// ── PRODUK: INFINITE SCROLL + SEARCH (FIX ANTI-LAG) ──
// Server cuma ngirim PRODUCTS_PAGE_SIZE produk pertama saat render awal
// (lihat GET /admin di server.js). Sisanya dimuat di sini per-batch lewat
// GET /admin/products?offset=&limit=&search= supaya jumlah produk yang bisa
// ditampung TIDAK dibatasi ukuran HTML/DOM awal -- baik seribu produk atau
// sepuluh, initial load beratnya sama.
const PROD_PAGE_SIZE = 30;
let _prodOffset = <%= productsPage.length %>;
let _prodSearchQuery = '';
let _prodSearchDebounce = null;
let _prodLoading = false;

function escapeHtmlAttr(s) {
    return String(s || '').replace(/&/g, '&amp;').replace(/"/g, '&quot;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
}

// Bikin markup 1 kartu produk PERSIS sama strukturnya dengan versi EJS
// (lihat blok <% productsPage.forEach %> di atas) supaya kartu hasil
// infinite-scroll konsisten tampilannya dengan kartu hasil render awal.
function renderProductCardHTML(p) {
    const pricing = p.pricingOptions || (p.items || []).map(i => ({ days: parseInt((i.l.match(/(\d+)/) || [])[1] || 0), price: i.p })) || [];
    const minPrice = pricing.length ? Math.min(...pricing.map(o => o.price)) : 0;
    const pricingJsonAttr = escapeHtmlAttr(JSON.stringify(pricing));
    const nameAttr = escapeHtmlAttr(p.name || '');
    const keys = p.keys || [];
    const pricingTags = pricing.map(po => {
        const cnt = keys.filter(k => k.endsWith(':' + po.days)).length;
        return `<span class="px-1.5 py-0.5 rounded text-[9px] font-bold ${cnt > 0 ? 'text-emerald-400 bg-emerald-500/10' : 'text-zinc-600 bg-zinc-800/50'}">${po.days}d: ${cnt}</span>`;
    }).join('');
    const genericCnt = keys.filter(k => !k.includes(':')).length;
    const genericTag = genericCnt > 0 ? `<span class="px-1.5 py-0.5 rounded text-[9px] font-bold text-yellow-400 bg-yellow-500/10">G: ${genericCnt}</span>` : '';
    const imgInner = p.image
        ? `<img src="${p.image}" alt="${nameAttr}" class="w-full h-full object-cover" loading="lazy" onerror="this.style.display='none'">`
        : `<div class="w-full h-full flex items-center justify-center"><iconify-icon icon="mdi:gamepad-variant" class="text-xl text-purple-400"></iconify-icon></div>`;

    return `
    <div class="prod-card fade-in" data-product-id="${p.id}">
        <div class="w-12 h-12 rounded-xl overflow-hidden flex-shrink-0 border border-white/10 bg-purple-500/10">${imgInner}</div>
        <div class="flex-1 min-w-0">
            <div class="flex items-center gap-2 mb-0.5 flex-wrap">
                <span class="text-sm font-semibold text-white truncate">${p.name || ''}</span>
                <span class="badge ${p.status === 'active' ? 'badge-done' : 'badge-inactive'}">${p.status === 'active' ? 'Aktif' : 'Off'}</span>
            </div>
            <div class="flex items-center gap-2 text-[10px] text-zinc-500 flex-wrap">
                <span class="px-1.5 py-0.5 rounded bg-purple-500/15 text-purple-400 font-bold uppercase">${p.category || ''}</span>
                <span class="prod-key-count ${keys.length > 0 ? 'text-emerald-400' : 'text-red-400'}"><iconify-icon icon="mdi:key" class="align-middle"></iconify-icon> ${keys.length} key</span>
                <span>${p.sold || 0} terjual</span>
                <span class="text-zinc-500 flex items-center gap-1 flex-wrap">${pricingTags}${genericTag}</span>
            </div>
            ${minPrice > 0 ? `<div class="text-[10px] text-purple-400 font-bold mt-0.5">Mulai Rp ${minPrice.toLocaleString('id-ID')}</div>` : ''}
        </div>
        <div class="flex flex-col gap-1.5 flex-shrink-0">
            <a href="/admin/product-edit?id=${p.id}" class="btn-warning py-1 px-2 text-[10px] text-center"><iconify-icon icon="mdi:pencil"></iconify-icon> Edit</a>
            <button data-pid="${p.id}" data-name="${nameAttr}" data-count="${keys.length}" data-pricing="${pricingJsonAttr}"
                onclick="openAddKeysModal(this.dataset.pid, this.dataset.name, parseInt(this.dataset.count), this.dataset.pricing)"
                class="btn-success py-1 px-2 text-[10px]"><iconify-icon icon="mdi:key-plus"></iconify-icon> Key</button>
            <button data-pid="${p.id}" data-name="${nameAttr}" data-pricing="${pricingJsonAttr}"
                onclick="openResellerPriceModal(this.dataset.pid, this.dataset.name, this.dataset.pricing)"
                class="py-1 px-2 text-[10px] font-bold rounded-lg flex items-center gap-1 justify-center"
                style="background:rgba(234,179,8,.15);border:1px solid rgba(234,179,8,.35);color:#facc15"><iconify-icon icon="mdi:crown"></iconify-icon> Reseller</button>
            <button onclick="deleteProduct('${p.id}')" class="btn-danger py-1 px-2 text-[10px]"><iconify-icon icon="mdi:trash-can"></iconify-icon></button>
        </div>
    </div>`;
}

async function loadMoreProducts() {
    if (_prodLoading) return;
    _prodLoading = true;
    const btn = document.getElementById('loadMoreProductsBtn');
    const originalHtml = btn.innerHTML;
    btn.innerHTML = '<iconify-icon icon="mdi:loading" class="animate-spin"></iconify-icon> Memuat...';
    try {
        const params = new URLSearchParams({ offset: _prodOffset, limit: PROD_PAGE_SIZE });
        if (_prodSearchQuery) params.set('search', _prodSearchQuery);
        const res = await fetch(`/admin/products?${params.toString()}`);
        const d = await res.json();
        if (d.success) {
            const list = document.getElementById('products-list');
            d.data.forEach(p => list.insertAdjacentHTML('beforeend', renderProductCardHTML(p)));
            _prodOffset += d.data.length;
            btn.style.display = d.hasMore ? '' : 'none';
        }
    } finally {
        btn.innerHTML = originalHtml;
        _prodLoading = false;
    }
}

// Search server-side (bukan filter di client) supaya tetap ringan walau
// katalog sudah sangat besar -- client tidak pernah nyimpen SEMUA produk
// di memori sekaligus untuk sekadar cari 1 nama.
function onProductSearchInput(value) {
    clearTimeout(_prodSearchDebounce);
    _prodSearchDebounce = setTimeout(() => runProductSearch(value.trim()), 300);
}

async function runProductSearch(query) {
    _prodSearchQuery = query;
    _prodOffset = 0;
    const list = document.getElementById('products-list');
    const emptyMsg = document.getElementById('products-search-empty');
    const btn = document.getElementById('loadMoreProductsBtn');
    list.innerHTML = '';
    emptyMsg.style.display = 'none';
    const params = new URLSearchParams({ offset: 0, limit: PROD_PAGE_SIZE });
    if (query) params.set('search', query);
    const res = await fetch(`/admin/products?${params.toString()}`);
    const d = await res.json();
    if (d.success) {
        if (!d.data.length) {
            emptyMsg.style.display = '';
            btn.style.display = 'none';
        } else {
            d.data.forEach(p => list.insertAdjacentHTML('beforeend', renderProductCardHTML(p)));
            _prodOffset = d.data.length;
            btn.style.display = d.hasMore ? '' : 'none';
        }
    }
}

async function toggleProduct(id) {
    const d = await api(`/admin/product/toggle/${id}`, {});
    if (d.success) {
        toast('Status diubah', 'success');
        const card = document.querySelector(`[data-product-id="${id}"]`);
        if (card) {
            const badge = card.querySelector('.badge');
            if (badge) {
                badge.className = d.status === 'active' ? 'badge badge-done' : 'badge badge-inactive';
                badge.textContent = d.status === 'active' ? 'Aktif' : 'Off';
            }
        }
    } else toast(d.message, 'error');
}

async function deleteProduct(id) {
    if (!confirm('Hapus produk ini?')) return;
    const d = await api(`/admin/product/delete/${id}`, {});
    if (d.success) {
        toast('Produk dihapus', 'warn');
        const card = document.querySelector(`[data-product-id="${id}"]`);
        if (card) { card.style.transition='opacity .3s,transform .3s'; card.style.opacity='0'; card.style.transform='translateX(20px)'; setTimeout(()=>card.remove(),300); }
    } else toast(d.message, 'error');
}

// bulkSetAutoStock() dihapus -- lihat catatan di HTML tombolnya (pane-products).

// ── TRANSAKSI ──
async function deleteTransaction(id) {
    if (!confirm('Hapus transaksi ini?')) return;
    const d = await api(`/admin/transaction/delete/${id}`, {});
    if (d.success) {
        toast('Transaksi dihapus', 'warn');
        const card = document.querySelector(`[data-trx-id="${id}"]`);
        if (card) { card.style.transition='opacity .3s'; card.style.opacity='0'; setTimeout(()=>card.remove(),300); }
    } else toast(d.message, 'error');
}

// ── USERS ──
async function deleteUser(id, name) {
    if (!confirm(`Hapus user "${name}"?`)) return;
    const d = await api(`/admin/user/delete/${id}`, {});
    if (d.success) {
        toast('User dihapus', 'warn');
        const card = document.querySelector(`[data-user-id="${id}"]`);
        if (card) { card.style.transition='opacity .3s'; card.style.opacity='0'; setTimeout(()=>card.remove(),300); }
    } else toast(d.message, 'error');
}

// ── TESTIMONI ──
// Cegah stored XSS: testimoni datang dari user (bisa siapa saja yang pernah
// beli 1 produk termurah), jadi WAJIB di-escape sebelum masuk innerHTML.
// Kalau tidak, user jahat bisa nitip payload script di kolom ulasan yang
// bakal jalan otomatis di browser admin begitu tab Testimoni dibuka —
// dan dari situ bisa nyuri sesi admin / manggil API admin lain diam-diam.
function escapeHtml(str) {
    return String(str ?? '').replace(/[&<>"']/g, c => ({
        '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;'
    }[c]));
}

async function loadTestimonials() {
    const el = document.getElementById('testi-list');
    el.innerHTML = '<div class="text-center py-8 text-zinc-600"><iconify-icon icon="mdi:loading" class="text-2xl animate-spin block mx-auto mb-2 text-purple-400"></iconify-icon><p class="text-xs">Memuat...</p></div>';
    try {
        const list = await (await fetch('/api/testimonials')).json();
        if (!list.length) { el.innerHTML = '<div class="text-center py-10 text-zinc-600 text-xs">Belum ada testimoni</div>'; return; }
        el.innerHTML = list.map(t => {
            const stars = '★'.repeat(t.rating) + '☆'.repeat(5-t.rating);
            const date  = new Date(t.date).toLocaleDateString('id-ID');
            const rawText = t.text || '';
            const short = escapeHtml(rawText.length > 80 ? rawText.slice(0,80)+'...' : rawText);
            const name = escapeHtml(t.name || t.username || 'Anonymous');
            return `
            <div class="neon-card p-3.5 fade-in">
                <div class="flex items-start justify-between gap-2 mb-2">
                    <div class="flex items-center gap-2">
                        <div class="w-8 h-8 rounded-full bg-gradient-to-br from-purple-600 to-indigo-700 flex items-center justify-center text-white text-xs font-bold flex-shrink-0">
                            ${name.charAt(0).toUpperCase()}
                        </div>
                        <div>
                            <div class="text-xs font-semibold text-white">${name}</div>
                            <div class="text-yellow-400 text-xs">${stars}</div>
                        </div>
                    </div>
                    <div class="flex items-center gap-1.5 flex-shrink-0">
                        ${t.verified ? '<span class="badge badge-done">✓ Verified</span>' : '<span class="text-[9px] font-black px-1.5 py-0.5 rounded" style="background:rgba(234,179,8,.15);color:#fbbf24;border:1px solid rgba(234,179,8,.3)">PENDING</span>'}
                        ${t.featured ? '<span class="badge badge-featured">★ Home</span>' : ''}
                    </div>
                </div>
                <p class="text-[11px] text-zinc-400 leading-relaxed mb-2">"${short}"</p>
                <div class="flex items-center justify-between gap-2 flex-wrap">
                    <div class="text-[10px] text-zinc-600">${escapeHtml(t.product||'Umum')} · ${date}</div>
                    <div class="flex gap-1.5 flex-wrap">
                        <button onclick="toggleVerified('${t.id}')" class="py-1 px-2 text-[10px] rounded-lg font-bold transition-all" style="${t.verified ? 'background:rgba(16,185,129,.15);color:#34d399;border:1px solid rgba(16,185,129,.3);' : 'background:rgba(234,179,8,.1);color:#fbbf24;border:1px solid rgba(234,179,8,.25);'}">${t.verified ? '✓ Verified' : '○ Verifikasi'}</button>
                        <button onclick="toggleFeatured('${t.id}')" class="btn-warning py-1 px-2 text-[10px]">${t.featured ? 'Unfeature' : '★ Feature'}</button>
                        <button onclick="deleteTestimonial('${t.id}')" class="btn-danger py-1 px-2 text-[10px]"><iconify-icon icon="mdi:trash-can"></iconify-icon></button>
                    </div>
                </div>
            </div>`;
        }).join('');
    } catch(err) {
        el.innerHTML = '<div class="text-center py-6 text-xs text-red-400">Gagal memuat testimoni</div>';
    }
}

document.getElementById('addTestimonialForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    const fd = new FormData(this);
    const data = {
        name: fd.get('name'), rating: fd.get('rating'), text: fd.get('text'),
        product: fd.get('product'), verified: fd.get('verified')==='on', featured: fd.get('featured')==='on'
    };
    const d = await api('/admin/testimonial/add', data);
    if (d.success) { toast('Testimoni ditambahkan!','success'); closeModal('modal-testi'); this.reset(); loadTestimonials(); }
    else toast(d.message, 'error');
});

async function deleteTestimonial(id) {
    if (!confirm('Hapus testimoni ini?')) return;
    const d = await api(`/admin/testimonial/delete/${id}`, {});
    if (d.success) { toast('Testimoni dihapus','warn'); loadTestimonials(); }
    else toast(d.message, 'error');
}

async function toggleFeatured(id) {
    const d = await api(`/admin/testimonial/toggle-featured/${id}`, {});
    if (d.success) loadTestimonials();
    else toast(d.message, 'error');
}

async function toggleVerified(id) {
    const d = await api(`/admin/testimonial/toggle-verified/${id}`, {});
    if (d.success) { toast(d.message || 'Status updated','success'); loadTestimonials(); }
    else toast(d.message, 'error');
}

// ── SETTINGS ──
document.getElementById('settingsForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    const fd = new FormData(this);
    const data = Object.fromEntries(fd);
    // Checkbox cuma muncul di FormData kalau dicentang — set eksplisit di sini
    // supaya kondisi "off" (default) juga selalu ikut terkirim & tersimpan.
    data.singleDeviceAdminLogin = document.getElementById('singleDeviceAdminLoginToggle').checked ? 'true' : 'false';
    data.showPopularSection = document.getElementById('showPopularSectionToggle').checked ? 'true' : 'false';
    const d = await api('/admin/settings/update', data);
    if (d.success) toast('Pengaturan disimpan!','success');
    else toast(d.message, 'error');
});

// ── QRIS MODE (3 pilihan: static / pakasir / genspay — cuma 1 yang aktif) ──
function setQrisMode(mode) {
    document.getElementById('qrisModeVal').value = mode;
    document.getElementById('sectionStatic').classList.toggle('hidden', mode !== 'static');
    document.getElementById('sectionPakasir').classList.toggle('hidden', mode !== 'pakasir');
    document.getElementById('sectionGenspay').classList.toggle('hidden', mode !== 'genspay');

    const btnClass = (active) => `py-2.5 rounded-xl text-[11px] font-bold border transition-all flex flex-col items-center justify-center gap-1 ${active ? 'bg-purple-500/20 border-indigo-500/50 text-purple-300' : 'bg-white/3 border-white/10 text-zinc-500'}`;
    document.getElementById('modeStaticBtn').className = btnClass(mode === 'static');
    document.getElementById('modePakasirBtn').className = btnClass(mode === 'pakasir');
    document.getElementById('modeGenspayBtn').className = btnClass(mode === 'genspay');

    // Auto-save mode — pakai endpoint gateway yang SEDANG dipilih, sertakan
    // credential gateway itu sendiri (bukan gateway lain) supaya tidak
    // ketimpa kosong.
    if (mode === 'pakasir') {
        api('/admin/settings/pakasir', { qrisMode: mode,
            apiKey: '<%= settings.pakasir?.apiKey||"" %>',
            project: '<%= settings.pakasir?.project||"" %>',
            apiBaseUrl: '<%= settings.pakasir?.apiBaseUrl||"app.pakasir.com" %>' })
            .then(d => { if(d.success) toast('Mode QRIS: Pakasir API', 'success'); });
    } else if (mode === 'genspay') {
        api('/admin/settings/genspay', { qrisMode: mode,
            apiKey: '<%= settings.genspay?.apiKey||"" %>',
            baseUrl: '<%= settings.genspay?.baseUrl||"https://genspay.my.id/api/v1" %>' })
            .then(d => { if(d.success) toast('Mode QRIS: GensPay API', 'success'); });
    } else {
        // static: cukup simpan lewat endpoint genspay (endpoint mana saja
        // sebenarnya oke karena keduanya menyimpan field qrisMode yang sama)
        api('/admin/settings/genspay', { qrisMode: mode,
            apiKey: '<%= settings.genspay?.apiKey||"" %>',
            baseUrl: '<%= settings.genspay?.baseUrl||"https://genspay.my.id/api/v1" %>' })
            .then(d => { if(d.success) toast('Mode QRIS: Statis', 'success'); });
    }
}

document.getElementById('pakasirForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    const fd = new FormData(this);
    const data = Object.fromEntries(fd);
    data.qrisMode = 'pakasir';
    const d = await api('/admin/settings/pakasir', data);
    if (d.success) toast('Pengaturan Pakasir disimpan!','success');
    else toast(d.message, 'error');
});

document.getElementById('genspayForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    const fd = new FormData(this);
    const data = Object.fromEntries(fd);
    data.qrisMode = 'genspay';
    const d = await api('/admin/settings/genspay', data);
    if (d.success) toast('Pengaturan GensPay disimpan!','success');
    else toast(d.message, 'error');
});

document.getElementById('qrisStaticForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    const fd = new FormData(this);
    if (!fd.get('qrisImage') || !fd.get('qrisImage').size) {
        toast('Pilih file gambar QRIS terlebih dahulu', 'error'); return;
    }
    const btn = this.querySelector('button[type=submit]');
    btn.disabled = true; btn.innerHTML = '<iconify-icon icon="mdi:loading" class="animate-spin"></iconify-icon> Mengupload...';
    const res = await fetch('/admin/qris/upload', { method: 'POST', body: fd });
    const d = await res.json();
    btn.disabled = false; btn.innerHTML = '<iconify-icon icon="mdi:upload"></iconify-icon> Upload Gambar QRIS';
    if (d.success) {
        toast('QRIS berhasil diupload! ✓', 'success');
        // Juga set mode ke statis dan save
        await api('/admin/settings/genspay', { qrisMode: 'static',
            apiKey: '<%= settings.genspay?.apiKey||"" %>',
            baseUrl: '<%= settings.genspay?.baseUrl||"https://genspay.my.id/api/v1" %>' });
        setTimeout(() => location.reload(), 900);
    } else toast(d.message || 'Gagal upload', 'error');
});

// PENTING: masing-masing form Pakasir/GensPay punya scope query sendiri
// (this.querySelector, bukan document.querySelector) — supaya kalau ada 2
// input dengan name="apiKey" di halaman yang sama (satu di form Pakasir,
// satu di form GensPay), test button selalu ambil value dari FORM-NYA
// SENDIRI, tidak pernah ambil form yang lain secara tidak sengaja.
async function testPakasir() {
    const form = document.getElementById('pakasirForm');
    const apiKey = form.querySelector('[name=apiKey]').value;
    const project = form.querySelector('[name=project]').value;
    const apiBaseUrl = form.querySelector('[name=apiBaseUrl]').value;
    const el = document.getElementById('testResultPakasir');
    el.className = 'text-[11px] p-2.5 rounded-lg bg-purple-500/10 border border-purple-500/20 text-purple-300';
    el.textContent = 'Menguji koneksi...';
    el.classList.remove('hidden');
    const d = await api('/admin/qris/test', { gateway: 'pakasir', apiKey, project, apiBaseUrl });
    if (d.success) {
        el.className = 'text-[11px] p-2.5 rounded-lg bg-emerald-500/10 border border-emerald-500/20 text-emerald-300';
        el.textContent = '✓ Koneksi berhasil! API siap digunakan.';
    } else {
        el.className = 'text-[11px] p-2.5 rounded-lg bg-red-500/10 border border-red-500/20 text-red-400';
        el.textContent = '✗ ' + (d.message || 'Koneksi gagal');
    }
}

async function testGenspay() {
    const form = document.getElementById('genspayForm');
    const apiKey = form.querySelector('[name=apiKey]').value;
    const baseUrl = form.querySelector('[name=baseUrl]').value;
    const el = document.getElementById('testResultGenspay');
    el.className = 'text-[11px] p-2.5 rounded-lg bg-purple-500/10 border border-purple-500/20 text-purple-300';
    el.textContent = 'Menguji koneksi...';
    el.classList.remove('hidden');
    const d = await api('/admin/qris/test', { gateway: 'genspay', apiKey, baseUrl });
    if (d.success) {
        el.className = 'text-[11px] p-2.5 rounded-lg bg-emerald-500/10 border border-emerald-500/20 text-emerald-300';
        el.textContent = '✓ Koneksi berhasil! API siap digunakan.';
    } else {
        el.className = 'text-[11px] p-2.5 rounded-lg bg-red-500/10 border border-red-500/20 text-red-400';
        el.textContent = '✗ ' + (d.message || 'Koneksi gagal');
    }
}

// Card & tombol "Reseller API (vipibmstore)" udah gak dirender lagi di atas
// (lihat komentar di HTML) -- listener submit-nya ikut dihapus juga di sini,
// soalnya document.getElementById('resellerApiForm') bakal balikin null kalau
// elemennya gak ada, dan .addEventListener(null) bikin SELURUH blok <script>
// ini gagal jalan (persis kejadian bug switchTab kemarin). testResellerApi()
// juga ikut dihapus karena udah gak ada tombol yang manggil dia.

document.getElementById('ghostSellerApiForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    const fd = new FormData(this);
    const data = Object.fromEntries(fd);
    const d = await api('/admin/settings/ghostseller-api', data);
    if (d.success) toast('Kredensial GhostSeller disimpan!','success');
    else toast(d.message, 'error');
});

async function testGhostSellerApi() {
    const form = document.getElementById('ghostSellerApiForm');
    const apiKey = form.querySelector('[name=apiKey]').value;
    const baseUrl = form.querySelector('[name=baseUrl]').value;
    const el = document.getElementById('testResultGhostSellerApi');
    el.className = 'text-[11px] p-2.5 rounded-lg bg-purple-500/10 border border-purple-500/20 text-purple-300';
    el.textContent = 'Menguji koneksi...';
    el.classList.remove('hidden');
    const d = await api('/admin/ghostseller-api/test', { apiKey, baseUrl });
    if (d.success) {
        el.className = 'text-[11px] p-2.5 rounded-lg bg-emerald-500/10 border border-emerald-500/20 text-emerald-300';
        el.textContent = `✓ Koneksi berhasil! ${d.productCount||0} produk ditemukan di katalog GhostSeller.`;
    } else {
        el.className = 'text-[11px] p-2.5 rounded-lg bg-red-500/10 border border-red-500/20 text-red-400';
        const detail = [d.status ? `HTTP ${d.status}` : null, d.code || null].filter(Boolean).join(' · ');
        el.textContent = '✗ ' + (d.message || 'Koneksi gagal') + (detail ? ` (${detail})` : '');
    }
}


document.getElementById('credForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    const fd = new FormData(this);
    const adminUsername = fd.get('adminUsername');
    const newPassword   = fd.get('newPassword');

    const s = await api('/admin/settings/update', { adminUsername });
    if (!s.success) { toast(s.message,'error'); return; }

    if (newPassword && newPassword.trim()) {
        const p = await api('/admin/settings/password', { newPassword });
        if (!p.success) { toast(p.message,'error'); return; }
    }

    toast('Kredensial diperbarui! Silakan login ulang.','success');
    setTimeout(() => { localStorage.removeItem('hm_cart'); window.location.href='/logout'; }, 1500);
});

// Sama seperti helper failLabel di sisi server (lihat views/pages/admin.ejs
// bagian render kartu transaksi) -- dipakai supaya badge hasil retry AJAX
// juga tidak selalu bilang "Stok Habis" kalau penyebabnya beda (error API,
// belum dimapping, dll).
function failLabelFromReason(reason) {
    if (!reason) return '❌ Gagal (Stok Habis)';
    if (/status \d{3}|timeout|network|koneksi|ECONN|gagal menghubungi/i.test(reason)) return '❌ Gagal (Error Koneksi API)';
    if (/mapping|tidak ditemukan produk provider/i.test(reason)) return '❌ Gagal (Belum Dimapping)';
    if (/stok habis|tidak ada key tersedia/i.test(reason)) return '❌ Gagal (Stok Habis)';
    return '❌ Gagal (Perlu Dicek)';
}

// ── KONFIRMASI TRANSAKSI ──
async function confirmTransaction(id) {
    if (!confirm('Konfirmasi pembayaran transaksi ini?')) return;
    const btn = document.getElementById(`trx-confirm-${id}`);
    if (btn) { btn.disabled = true; btn.innerHTML = '<iconify-icon icon="mdi:loading" class="animate-spin"></iconify-icon> Proses...'; }
    const d = await api(`/admin/transaction/confirm/${id}`, {});
    if (d.success && !d.outOfStock) {
        toast(d.type === 'reseller' ? 'User berhasil diupgrade jadi Reseller VIP!' : d.type === 'deposit' ? `Top up saldo dikonfirmasi! Saldo user: Rp ${(d.balance||0).toLocaleString('id-ID')}` : `Konfirmasi berhasil! Key: ${d.key||'-'}`, 'success');
        const badge = document.getElementById(`trx-status-badge-${id}`);
        if (badge) { badge.className = 'badge badge-done'; badge.textContent = 'Done'; }
        if (btn) btn.remove();
        const card = document.querySelector(`[data-trx-id="${id}"]`);
        if (card) { card.setAttribute('data-status','done'); card.setAttribute('data-outofstock',''); }
    } else if (d.success && d.outOfStock) {
        // Retry masih gagal (stok masih habis) -- tetap tandai gagal, jangan
        // dianggap sukses walau HTTP-nya success:true.
        toast(d.warning || 'Stok masih habis, key belum terkirim', 'error');
        const badge = document.getElementById(`trx-status-badge-${id}`);
        if (badge) { badge.className = 'badge badge-failed'; badge.textContent = failLabelFromReason(d.warning); badge.title = d.warning || ''; }
        if (btn) { btn.disabled = false; btn.innerHTML = '<iconify-icon icon="mdi:refresh"></iconify-icon> Coba Lagi'; }
        const card = document.querySelector(`[data-trx-id="${id}"]`);
        if (card) { card.setAttribute('data-status','done'); card.setAttribute('data-outofstock','failed'); }
    } else {
        toast(d.message || 'Gagal konfirmasi', 'error');
        if (btn) { btn.disabled = false; btn.innerHTML = '<iconify-icon icon="mdi:check-circle"></iconify-icon> Konfirmasi Bayar'; }
    }
}

// ── TOGGLE VISIBILITY KEY (sensor) ──
// Key disembunyikan default (data-masked="true") supaya tidak keliatan
// sekilas lewat/screen-share; klik ikon mata untuk tampilkan/sembunyikan
// per baris. Nilai asli disimpan di data-real, tidak pernah dihapus dari
// DOM, cuma teks yang ditampilkan yang diganti.
function toggleKeyVisibility(btn) {
    const span = btn.previousElementSibling;
    if (!span || !span.classList.contains('key-value')) return;
    const masked = span.getAttribute('data-masked') !== 'false';
    if (masked) {
        span.textContent = span.getAttribute('data-real');
        span.setAttribute('data-masked', 'false');
        btn.innerHTML = '<iconify-icon icon="mdi:eye-off-outline" style="font-size:13px"></iconify-icon>';
    } else {
        span.textContent = '••••••••';
        span.setAttribute('data-masked', 'true');
        btn.innerHTML = '<iconify-icon icon="mdi:eye-outline" style="font-size:13px"></iconify-icon>';
    }
}

// ── FILTER TRANSAKSI (status + lacak key digabung) ──
// Kedua filter (tombol status & search box lacak key) harus bisa jalan
// BARENGAN -- misal cari key tertentu tapi cuma di antara yang statusnya
// "Done" -- makanya state keduanya disimpan & re-apply tiap ada perubahan,
// bukan cuma override satu sama lain.
let _trxStatusFilter = 'all';
let _trxKeyQuery = '';

function filterTrx(status) {
    _trxStatusFilter = status;
    document.querySelectorAll('.trx-filter').forEach(b => {
        b.className = 'trx-filter px-2.5 py-1 rounded-lg text-[10px] font-bold bg-white/3 border border-white/10 text-zinc-500';
    });
    const activeBtn = document.getElementById(`trxf-${status}`);
    if (activeBtn) activeBtn.className = 'trx-filter px-2.5 py-1 rounded-lg text-[10px] font-bold bg-purple-500/20 border border-indigo-500/40 text-purple-300';
    applyTrxFilters();
}

function filterTrxByKey(query) {
    _trxKeyQuery = (query || '').trim().toLowerCase();
    applyTrxFilters();
}

function applyTrxFilters() {
    const status = _trxStatusFilter;
    const q = _trxKeyQuery;
    let visibleCount = 0;
    document.querySelectorAll('.trx-item').forEach(card => {
        const s = card.getAttribute('data-status');
        const oos = card.getAttribute('data-outofstock');
        let matchesStatus;
        if (status === 'failed') matchesStatus = (oos === 'failed');
        else if (status === 'done') matchesStatus = (s === 'done' && oos !== 'failed');
        else matchesStatus = (status === 'all' || s === status);

        const key = card.getAttribute('data-key') || '';
        const matchesKey = !q || key.includes(q);

        const show = matchesStatus && matchesKey;
        card.style.display = show ? '' : 'none';
        if (show) visibleCount++;
    });

    // Tampilkan pesan "tidak ditemukan" khusus saat lagi nyari key spesifik
    // supaya admin tahu ini bukan "belum ada transaksi" tapi "key tidak ada".
    let emptyMsg = document.getElementById('trx-key-empty-msg');
    if (q && visibleCount === 0) {
        if (!emptyMsg) {
            emptyMsg = document.createElement('div');
            emptyMsg.id = 'trx-key-empty-msg';
            emptyMsg.className = 'text-center py-8 text-zinc-600 text-xs';
            document.getElementById('trx-list').appendChild(emptyMsg);
        }
        emptyMsg.textContent = `Tidak ada transaksi dengan key mengandung "${q}"`;
        emptyMsg.style.display = '';
    } else if (emptyMsg) {
        emptyMsg.style.display = 'none';
    }
}

// ── TOGGLE RESELLER USER ──
async function toggleReseller(id, username) {
    const action = document.getElementById(`reseller-btn-${id}`)?.textContent?.trim().includes('Cabut') ? 'cabut' : 'jadikan';
    if (!confirm(`${action === 'cabut' ? 'Cabut' : 'Jadikan'} "${username}" sebagai reseller?`)) return;
    const d = await api(`/admin/user/toggle-reseller/${id}`, {});
    if (d.success) {
        toast(d.is_reseller ? `${username} sekarang Reseller VIP!` : `Status reseller ${username} dicabut`, d.is_reseller ? 'success' : 'warn');
        setTimeout(() => location.reload(), 800);
    } else toast(d.message, 'error');
}

// ── ADJUST SALDO WALLET USER ──
// ── USER SEARCH ──
function filterUsers(q) {
    const term = q.trim().toLowerCase();
    const cards = document.querySelectorAll('.user-card');
    let visible = 0;
    cards.forEach(card => {
        const hay = card.getAttribute('data-search') || '';
        const match = !term || hay.includes(term);
        card.style.display = match ? '' : 'none';
        if (match) visible++;
    });
    const empty = document.getElementById('userSearchEmpty');
    if (empty) empty.classList.toggle('hidden', visible > 0 || !term);
}

async function adjustBalance(id, username) {
    const input = prompt(`Tambah/kurangi saldo "${username}"\nIsi angka positif untuk menambah, negatif untuk mengurangi.\nContoh: 50000 atau -20000`);
    if (input === null || input.trim() === '') return;
    const amount = parseInt(input);
    if (isNaN(amount) || amount === 0) { toast('Nominal tidak valid', 'error'); return; }
    const d = await api(`/admin/user/adjust-balance/${id}`, { amount });
    if (d.success) {
        toast(`Saldo ${username} sekarang Rp ${d.balance.toLocaleString('id-ID')}`, 'success');
        setTimeout(() => location.reload(), 800);
    } else toast(d.message, 'error');
}

// ── ARCADE: kasih koin ke user pilihan ──
async function giveCoinToUser() {
    const sel = document.getElementById('coinUserSelect');
    const id = sel.value;
    if (!id) { toast('Pilih user dulu', 'error'); return; }
    const username = sel.options[sel.selectedIndex].dataset.username;
    const amount = parseInt(document.getElementById('coinAmountInput').value);
    if (isNaN(amount) || amount === 0) { toast('Jumlah koin tidak valid', 'error'); return; }
    const d = await api(`/admin/arcade/give-coin/${id}`, { amount });
    if (d.success) {
        toast(`Koin ${d.username} sekarang ${d.coins}`, 'success');
        setTimeout(() => location.reload(), 800);
    } else toast(d.message, 'error');
}

// ── ARCADE: tambah baris hadiah key baru ke tabel (belum tersimpan sampai klik Simpan) ──
function addKeyPrizeRow() {
    const tbody = document.querySelector('#prizeConfigTable tbody');
    const productOptions = window.__gnProducts || [];
    if (!productOptions.length) { toast('Tidak ada product untuk dijadikan hadiah key', 'error'); return; }
    const tr = document.createElement('tr');
    tr.dataset.prizeId = '';
    tr.dataset.prizeType = 'key';
    tr.innerHTML = `
        <td class="text-zinc-300">Free Key (baru)</td>
        <td><span class="text-[10px] font-bold px-2 py-0.5 rounded" style="background:rgba(250,204,21,.12);color:#facc15">Free Key</span></td>
        <td><input type="number" min="0" class="hm-input text-xs prize-weight" value="5"></td>
        <td>
            <select class="hm-input text-xs prize-product">
                ${productOptions.map(p => `<option value="${p.id}">${p.name} (${p.keyCount} key)</option>`).join('')}
            </select>
        </td>
        <td><button onclick="this.closest('tr').remove()" class="text-red-400 hover:text-red-300" title="Hapus hadiah ini"><iconify-icon icon="mdi:trash-can-outline"></iconify-icon></button></td>
    `;
    tbody.appendChild(tr);
}

// ── ARCADE: simpan konfigurasi hadiah gacha ──
async function saveArcadePrizes() {
    const rows = document.querySelectorAll('#prizeConfigTable tbody tr');
    const prizes = Array.from(rows).map(row => {
        const type = row.dataset.prizeType;
        const weightEl = row.querySelector('.prize-weight');
        const valueEl = row.querySelector('.prize-value');
        const productEl = row.querySelector('.prize-product');
        return {
            id: row.dataset.prizeId || undefined,
            type,
            weight: weightEl ? weightEl.value : undefined,
            value: valueEl ? valueEl.value : undefined,
            productId: productEl ? productEl.value : undefined
        };
    });
    const d = await api('/admin/arcade/save-prizes', { prizes });
    if (d.success) { toast('Pengaturan arcade disimpan!', 'success'); setTimeout(() => location.reload(), 800); }
    else toast(d.message, 'error');
}

// ── RESELLER SETTINGS ──
document.getElementById('resellerSettingsForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    const fd = new FormData(this);
    const data = {
        resellerEnabled: document.getElementById('resellerEnabledToggle').checked ? 'true' : 'false',
        resellerPrice: fd.get('resellerPrice'),
        resellerDiscount: fd.get('resellerDiscount'),
        resellerMinDeposit: fd.get('resellerMinDeposit'),
        resellerNote: fd.get('resellerNote')
    };
    const d = await api('/admin/settings/reseller', data);
    if (d.success) toast('Pengaturan reseller disimpan!', 'success');
    else toast(d.message, 'error');
});

document.querySelector('[name=resellerDiscount]')?.addEventListener('input', function() {
    const el = document.getElementById('discPreview');
    if (el) el.textContent = (this.value || 20) + '%';
});

// ─── Banner Carousel ───
function previewBannerFile(e) {
    const file = e.target.files[0];
    if (!file) return;
    const url = URL.createObjectURL(file);
    document.getElementById('bannerFileImg').src = url;
    document.getElementById('bannerFilePreview').classList.remove('hidden');
}
function previewBannerUrl(url) {
    const wrap = document.getElementById('bannerUrlPreview');
    const img  = document.getElementById('bannerUrlImg');
    if (url.trim()) { img.src = url.trim(); wrap.classList.remove('hidden'); }
    else wrap.classList.add('hidden');
}

async function loadBanners() {
    const res = await fetch('/api/banners');
    // load all (including inactive) via settings
    const sRes = await fetch('/admin/product/xxx').catch(()=>null); // dummy
    // get settings banners directly from rendered data
    renderBanners(window._allBanners || []);
}

function renderBanners(banners) {
    const el = document.getElementById('bannerList');
    if (!banners.length) { el.innerHTML = '<div class="text-xs text-zinc-600 text-center py-4">Belum ada banner. Tambah banner di atas.</div>'; return; }
    el.innerHTML = banners.map((b,i) => `
        <div class="flex items-center gap-3 p-3 rounded-xl border ${b.active!==false?'border-purple-500/20':'border-white/5 opacity-50'}" style="background:rgba(255,255,255,.02);">
            <div class="w-20 h-12 rounded-lg overflow-hidden flex-shrink-0 border border-white/10">
                <img src="${b.imageUrl}" class="w-full h-full object-cover" onerror="this.style.display='none'">
            </div>
            <div class="flex-1 min-w-0">
                <div class="text-xs font-bold text-white truncate">${b.title||'(tanpa judul)'}</div>
                <div class="text-[10px] text-zinc-500 truncate">${b.subtitle||''}</div>
                <div class="text-[10px] text-purple-400 truncate">${b.link||'/'}</div>
            </div>
            <div class="flex flex-col gap-1.5 flex-shrink-0">
                <button onclick="toggleBanner('${b.id}')"
                    class="px-2 py-1 rounded text-[10px] font-bold border transition-all ${b.active!==false?'border-emerald-500/30 text-emerald-400 bg-emerald-500/10':'border-zinc-600 text-zinc-500'}">
                    ${b.active!==false?'Aktif':'Mati'}
                </button>
                <button onclick="deleteBanner('${b.id}')"
                    class="px-2 py-1 rounded text-[10px] font-bold border border-red-500/30 text-red-400 bg-red-500/10 hover:bg-red-500/20 transition-all">
                    Hapus
                </button>
            </div>
        </div>`).join('');
}

async function addBanner(e) {
    e.preventDefault();
    const btn = document.getElementById('bannerAddBtn');
    btn.disabled = true;
    btn.innerHTML = '<iconify-icon icon="mdi:loading" class="animate-spin"></iconify-icon> Menyimpan...';
    const fd = new FormData(e.target);
    const res = await fetch('/admin/banners/add', { method: 'POST', body: fd });
    const d = await res.json();
    btn.disabled = false;
    btn.innerHTML = '<iconify-icon icon="mdi:plus-circle"></iconify-icon> Tambah Banner';
    if (d.success) {
        toast('Banner berhasil ditambahkan!', 'success');
        window._allBanners = d.banners;
        renderBanners(d.banners);
        e.target.reset();
        document.getElementById('bannerFilePreview').classList.add('hidden');
        document.getElementById('bannerUrlPreview').classList.add('hidden');
    } else toast(d.message || 'Gagal tambah banner', 'error');
}

async function toggleBanner(id) {
    const d = await api(`/admin/banners/toggle/${id}`, {});
    if (d.success) {
        window._allBanners = window._allBanners.map(b => b.id===id ? {...b, active:d.active} : b);
        renderBanners(window._allBanners);
        toast(`Banner ${d.active?'diaktifkan':'dinonaktifkan'}`, 'success');
    }
}

async function deleteBanner(id) {
    if (!confirm('Hapus banner ini?')) return;
    const d = await api(`/admin/banners/delete/${id}`, {});
    if (d.success) {
        window._allBanners = window._allBanners.filter(b => b.id!==id);
        renderBanners(window._allBanners);
        toast('Banner dihapus', 'success');
    }
}

// Override api() untuk DELETE yang butuh method POST tanpa body conflict
window.api = window.api || async function(url, data) {
    const res = await fetch(url, { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(data) });
    return res.json();
};

// Load banners saat tab settings dibuka
const _origSwitchTab = window.switchTab;
window.switchTab = function(name) {
    _origSwitchTab && _origSwitchTab(name);
    if (name === 'settings' && !window._bannersLoaded) {
        window._bannersLoaded = true;
        fetch('/api/banners').then(r=>r.json()).then(banners=>{
            // Fetch all banners including inactive via a trick:
            // We use the already-rendered EJS data
            window._allBanners = <%- JSON.stringify(settings.banners || []) %>;
            renderBanners(window._allBanners);
        });
    }
};

// Init jika sudah di tab settings
if (document.querySelector('#pane-settings.active')) {
    window._allBanners = <%- JSON.stringify(settings.banners || []) %>;
    renderBanners(window._allBanners);
}

document.getElementById('categoryForm').addEventListener('submit', async function(e) {
    e.preventDefault();
    const fd = new FormData(this);
    const raw = fd.get('categories') || '';
    const lines = raw.trim().split('\n').filter(l=>l.trim());
    const cats = [], labels = {};
    lines.forEach(line => {
        const [id, ...rest] = line.split(':');
        if (id && id.trim()) { cats.push(id.trim()); labels[id.trim()] = rest.join(':').trim()||id.trim(); }
    });
    if (!cats.length) { toast('Minimal 1 kategori diperlukan', 'error'); return; }
    const d = await api('/admin/settings/update', { categories: JSON.stringify(cats), categoryLabels: JSON.stringify(labels) });
    if (d.success) { toast('Kategori disimpan!', 'success'); setTimeout(()=>window.location.href='/admin#settings',800); }
    else toast(d.message, 'error');
});

// ═══ SALES CHART ═══
(function initSalesChart() {
    const ctx = document.getElementById('salesChart');
    if (!ctx || typeof Chart === 'undefined') return;
    const raw = <%- JSON.stringify(chartData) %>;
    const labels = raw.map(d => d.date);
    const counts = raw.map(d => d.count);
    const revenues = raw.map(d => d.revenue);
    new Chart(ctx, {
        type: 'bar',
        data: {
            labels,
            datasets: [
                {
                    label: 'Transaksi',
                    data: counts,
                    backgroundColor: 'rgba(124,47,214,.6)',
                    borderColor: 'rgba(124,47,214,1)',
                    borderWidth: 1,
                    borderRadius: 6,
                    yAxisID: 'y'
                },
                {
                    label: 'Revenue (K)',
                    data: revenues.map(r => Math.round(r/1000)),
                    type: 'line',
                    borderColor: 'rgba(52,211,153,.8)',
                    backgroundColor: 'rgba(52,211,153,.1)',
                    pointBackgroundColor: '#34d399',
                    pointRadius: 4,
                    tension: 0.4,
                    fill: true,
                    yAxisID: 'y2'
                }
            ]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            interaction: { mode: 'index', intersect: false },
            plugins: {
                legend: { labels: { color: '#a1a1aa', font: { size: 10 } } },
                tooltip: { backgroundColor: 'rgba(15,15,37,.95)', titleColor:'#fff', bodyColor:'#a1a1aa', borderColor:'rgba(124,47,214,.3)', borderWidth:1 }
            },
            scales: {
                x: { ticks: { color: '#52525b', font: { size: 9 } }, grid: { color: 'rgba(255,255,255,.04)' } },
                y: { ticks: { color: '#38bdf8', font: { size: 9 } }, grid: { color: 'rgba(124,47,214,.08)' }, title: { display: true, text: 'Transaksi', color: '#38bdf8', font: { size: 9 } } },
                y2: { position: 'right', ticks: { color: '#34d399', font: { size: 9 } }, grid: { drawOnChartArea: false }, title: { display: true, text: 'Revenue (K)', color: '#34d399', font: { size: 9 } } }
            }
        }
    });
})();

// ═══ KEY POOL MANAGEMENT ═══
let _allKeys = [];

async function loadKeys() {
    const r = await fetch('/admin/keyspool');
    const d = await r.json();
    if (!d.success) return;
    _allKeys = d.data || [];
    renderKeys(_allKeys);
}

function renderKeys(keys) {
    const list = document.getElementById('keysList');
    const total = document.getElementById('keyTotal');
    const stats = document.getElementById('keyStatsLabel');
    if (!list) return;
    const used = keys.filter(k => k.used).length;
    const avail = keys.length - used;
    if (total) total.textContent = keys.length;
    if (stats) stats.textContent = `${avail} tersedia · ${used} terpakai`;
    if (!keys.length) {
        list.innerHTML = '<div class="text-center py-8 text-zinc-600 text-xs"><iconify-icon icon="mdi:key-off" class="text-3xl block mx-auto mb-2 text-zinc-700"></iconify-icon>Belum ada key</div>';
        return;
    }
    list.innerHTML = keys.map(k => `
        <div class="neon-card p-3 flex items-center gap-3 ${k.used ? 'opacity-60' : ''}">
            <div class="w-8 h-8 rounded-lg flex items-center justify-center flex-shrink-0 ${k.used ? 'bg-zinc-800' : 'bg-purple-500/15 border border-indigo-500/25'}">
                <iconify-icon icon="${k.used ? 'mdi:key-off' : 'mdi:key'}" class="text-base ${k.used ? 'text-zinc-600' : 'text-purple-400'}"></iconify-icon>
            </div>
            <div class="flex-1 min-w-0">
                <div class="flex items-center gap-2 flex-wrap">
                    <span class="font-mono text-sm font-black text-white">${k.code}</span>
                    <span class="text-[10px] font-bold px-2 py-0.5 rounded ${k.used ? 'bg-zinc-700 text-zinc-500' : 'bg-purple-500/20 text-purple-400 border border-purple-500/30'}">
                        ${k.duration} Hari
                    </span>
                    ${k.used ? `<span class="text-[9px] bg-red-500/15 text-red-400 border border-red-500/25 px-1.5 py-0.5 rounded font-bold">TERPAKAI</span>` : '<span class="text-[9px] bg-emerald-500/15 text-emerald-400 border border-emerald-500/25 px-1.5 py-0.5 rounded font-bold">TERSEDIA</span>'}
                </div>
                <div class="text-[10px] text-zinc-500 mt-0.5">
                    ${k.label !== k.duration + ' Hari' ? k.label + ' · ' : ''}
                    ${k.used ? `Digunakan: <span class="text-zinc-400">${k.usedByUsername || '?'}</span>` : `Dibuat: ${new Date(k.createdAt).toLocaleDateString('id-ID')}`}
                </div>
            </div>
            <button onclick="deleteKey('${k.id}')" class="btn-danger py-1 px-2 text-[10px]" ${k.used ? 'disabled style="opacity:.4"' : ''}>
                <iconify-icon icon="mdi:trash-can"></iconify-icon>
            </button>
        </div>`).join('');
}

async function addKey() {
    const code = document.getElementById('newKeyCode').value.trim();
    const duration = document.getElementById('newKeyDuration').value.trim();
    const label = document.getElementById('newKeyLabel').value.trim();
    const note = document.getElementById('newKeyNote').value.trim();
    if (!code || !duration) { toast('Kode dan durasi wajib diisi', 'error'); return; }
    const d = await api('/admin/keyspool/add', { code, duration, label, note });
    if (d.success) {
        toast('Key berhasil ditambahkan!', 'success');
        document.getElementById('newKeyCode').value = '';
        document.getElementById('newKeyDuration').value = '';
        document.getElementById('newKeyLabel').value = '';
        document.getElementById('newKeyNote').value = '';
        _allKeys = d.data;
        renderKeys(_allKeys);
    } else toast(d.message, 'error');
}

async function generateKeys() {
    const count = document.getElementById('genCount').value;
    const duration = document.getElementById('genDuration').value;
    const prefix = document.getElementById('genPrefix').value;
    if (!duration || parseInt(duration) <= 0) { toast('Durasi tidak valid', 'error'); return; }
    const d = await api('/admin/keyspool/generate', { count, duration, prefix });
    if (d.success) {
        toast(`${d.generated} key berhasil dibuat!`, 'success');
        _allKeys = d.data;
        renderKeys(_allKeys);
        const res = document.getElementById('genResult');
        res.classList.remove('hidden');
        res.innerHTML = `<div class="p-3 rounded-xl text-xs font-mono text-purple-300 break-all" style="background:rgba(124,47,214,.08);border:1px solid rgba(124,47,214,.2)">${d.codes.join('<br>')}</div>`;
    } else toast(d.message, 'error');
}

async function deleteKey(id) {
    if (!confirm('Hapus key ini?')) return;
    const d = await api('/admin/keyspool/delete/' + id, {});
    if (d.success) {
        toast('Key dihapus', 'success');
        _allKeys = _allKeys.filter(k => k.id !== id);
        renderKeys(_allKeys);
    } else toast(d.message, 'error');
}

// ═══ VOUCHER MANAGEMENT ═══
async function loadVouchers() {
    const r = await fetch('/admin/vouchers');
    const d = await r.json();
    if (!d.success) return;
    renderVouchers(d.data || []);
}

function renderVouchers(vouchers) {
    const list = document.getElementById('vouchersList');
    const stats = document.getElementById('voucherStatsLabel');
    if (!list) return;
    const active = vouchers.filter(v => v.active).length;
    if (stats) stats.textContent = `${vouchers.length} total · ${active} aktif`;
    if (!vouchers.length) {
        list.innerHTML = '<div class="text-center py-8 text-zinc-600 text-xs"><iconify-icon icon="mdi:ticket-outline" class="text-3xl block mx-auto mb-2 text-zinc-700"></iconify-icon>Belum ada voucher</div>';
        return;
    }
    list.innerHTML = vouchers.map(v => {
        const expired = v.expiresAt && new Date(v.expiresAt) < new Date();
        const exhausted = v.maxUses > 0 && v.usedCount >= v.maxUses;
        const isInactive = !v.active || expired || exhausted;
        const valDisplay = v.type === 'percent' ? v.value + '%' : 'Rp ' + v.value.toLocaleString('id-ID');
        const minDisplay = v.minPurchase > 0 ? 'Min Rp ' + v.minPurchase.toLocaleString('id-ID') : 'Tanpa min';
        return `<div class="neon-card p-3 flex items-center gap-3 ${isInactive ? 'opacity-50' : ''}">
            <div class="w-9 h-9 rounded-lg flex items-center justify-center flex-shrink-0 ${v.active ? 'bg-green-500/15 border border-green-500/25' : 'bg-zinc-800'}">
                <iconify-icon icon="${v.active ? 'mdi:ticket-percent' : 'mdi:ticket-off'}" class="text-lg ${v.active ? 'text-green-400' : 'text-zinc-600'}"></iconify-icon>
            </div>
            <div class="flex-1 min-w-0">
                <div class="flex items-center gap-2 flex-wrap">
                    <span class="font-mono text-sm font-black text-white">${v.code}</span>
                    <span class="text-[10px] font-bold px-2 py-0.5 rounded ${v.active ? 'bg-green-500/20 text-green-400 border border-green-500/30' : 'bg-zinc-700 text-zinc-500'}">${valDisplay}</span>
                    ${v.excludeReseller ? '<span class="text-[9px] font-bold px-1.5 py-0.5 rounded" style="background:rgba(234,179,8,.12);color:#fbbf24;border:1px solid rgba(234,179,8,.25)">Non-Reseller</span>' : ''}
                </div>
                <div class="flex items-center gap-3 text-[10px] mt-0.5 flex-wrap">
                    <span class="text-zinc-500">${v.description || '—'}</span>
                    <span class="text-zinc-600">${minDisplay}</span>
                    <span class="text-zinc-600">${v.usedCount}/${v.maxUses || '∞'} dipakai</span>
                </div>
                ${v.expiresAt ? `<div class="text-[9px] ${expired ? 'text-red-400' : 'text-zinc-500'}">Kadaluarsa: ${new Date(v.expiresAt).toLocaleDateString('id-ID')}</div>` : ''}
            </div>
            <div class="flex flex-col gap-1">
                <button onclick="toggleVoucher('${v.id}')" class="px-2 py-1 text-[10px] rounded-lg border transition-all font-semibold ${v.active ? 'bg-yellow-500/15 border-yellow-500/30 text-yellow-400 hover:bg-yellow-500/25' : 'bg-emerald-500/15 border-emerald-500/30 text-emerald-400 hover:bg-emerald-500/25'}">
                    <iconify-icon icon="${v.active ? 'mdi:pause' : 'mdi:play'}"></iconify-icon>
                    ${v.active ? 'Nonaktif' : 'Aktifkan'}
                </button>
                <button onclick="deleteVoucher('${v.id}')" class="btn-danger py-1 px-2 text-[10px]">
                    <iconify-icon icon="mdi:trash-can"></iconify-icon>
                </button>
            </div>
        </div>`;
    }).join('');
}

async function addVoucher() {
    const code = document.getElementById('newVoucherCode').value.trim();
    const type = document.getElementById('newVoucherType').value;
    const value = document.getElementById('newVoucherValue').value;
    const minPurchase = document.getElementById('newVoucherMin').value;
    const maxUses = document.getElementById('newVoucherMax').value;
    const perUserLimit = document.getElementById('newVoucherPerUser').value;
    const expiresAt = document.getElementById('newVoucherExpiry').value;
    const description = document.getElementById('newVoucherDesc').value.trim();
    if (!code || !value) { toast('Kode dan nilai wajib diisi', 'error'); return; }
    const d = await api('/admin/vouchers/add', { code, type, value, minPurchase, maxUses, perUserLimit, expiresAt, description, excludeReseller: document.getElementById('newVoucherExcludeReseller').checked });
    if (d.success) {
        toast('Voucher berhasil dibuat!', 'success');
        document.getElementById('newVoucherCode').value = '';
        document.getElementById('newVoucherValue').value = '';
        document.getElementById('newVoucherDesc').value = '';
        renderVouchers(d.data);
    } else toast(d.message, 'error');
}

async function toggleVoucher(id) {
    const d = await api('/admin/vouchers/toggle/' + id, {});
    if (d.success) loadVouchers();
    else toast(d.message, 'error');
}

async function deleteVoucher(id) {
    if (!confirm('Hapus voucher ini?')) return;
    const d = await api('/admin/vouchers/delete/' + id, {});
    if (d.success) {
        toast('Voucher dihapus', 'success');
        loadVouchers();
    } else toast(d.message, 'error');
}

// ═══ EXPORT / IMPORT DATABASE ═══
async function importDB(event) {
    const file = event.target.files[0];
    const statusEl = document.getElementById('importStatus');
    if (!file) return;
    statusEl.classList.remove('hidden');
    statusEl.innerHTML = '<div class="flex items-center gap-2 text-xs text-purple-400"><iconify-icon icon="mdi:loading" class="animate-spin"></iconify-icon> Membaca file...</div>';
    try {
        const text = await file.text();
        const data = JSON.parse(text);
        const r = await fetch('/admin/import', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(data)
        });
        const d = await r.json();
        if (d.success) {
            statusEl.innerHTML = `<div class="flex items-center gap-2 text-xs text-emerald-400"><iconify-icon icon="mdi:check-circle"></iconify-icon> ${d.message}. Reload...</div>`;
            setTimeout(() => location.reload(), 1000);
        } else {
            statusEl.innerHTML = `<div class="flex items-center gap-2 text-xs text-red-400"><iconify-icon icon="mdi:alert-circle"></iconify-icon> ${d.message}</div>`;
        }
    } catch(e) {
        statusEl.innerHTML = `<div class="flex items-center gap-2 text-xs text-red-400"><iconify-icon icon="mdi:alert-circle"></iconify-icon> File tidak valid: ${e.message}</div>`;
    }
    event.target.value = '';
}
</script>
</body>
</html>
